# Sidebar navigation, rendered by _layouts/default.html.
#
# `path` is site-root-relative and does NOT carry the baseurl -- the layout prepends
# {{ site.baseurl }}. It is also what page.url is compared against to mark the current page, so the
# two have to stay in the same form: a docs/FOO.md page is built to /FOO.html, because no permalink
# style is configured in _config.yml. If a permalink style is ever added, every path here changes.
#
# `label` is the short nav name and follows the filename. `title` is the document's own H1 verbatim,
# which is not always the same thing, and is used as the link's title attribute.
#
# An entry with `url` instead of `path` is an absolute link off the site. Those exist because
# INSTALL.md and the scripts live at the repository root, outside the Jekyll source, so the site
# cannot serve them at all.
#
# A page in a SUBDIRECTORY follows the same rule: docs/standards/FOO.md is built to
# /standards/FOO.html. ONE FILENAME BREAKS THAT RULE, which is why a section index would be called
# OVERVIEW.md and not README.md. jekyll-optional-front-matter refuses to convert a front-matter-less
# file whose basename is on its FILENAME_BLACKLIST (README, LICENSE, CONTRIBUTING and friends), so a
# README stays a static file; jekyll-readme-index then republishes it as the DIRECTORY INDEX with
# permalink File.dirname(url) + "/", i.e. /standards/, and the raw .md is served as well. There is no
# /standards/README.html for a nav path to point at. Both plugins are in the GitHub Pages
# always-enabled set and cannot be turned off; the two config keys that would change the behaviour,
# readme_index.with_frontmatter and readme_index.remove_originals, are unset in _config.yml. Naming
# such a file OVERVIEW.md keeps it on the ordinary path, so its URL is predictable and page.url
# matches.
#
# THE GROUPS ARE ORDERED BY WHAT THE READER IS TRYING TO DO, not by what kind of artifact each page
# is. That was the change of 2026-08-16. Before it there were seven groups holding 25 links in which
# a reference page, an installation step and a case study sat side by side, and the reader who had
# just arrived and the reader debugging a merge were offered the same flat list. The order below is
# the order the work happens in: understand it, install it, run a build, keep it honest, look
# something up.

- group: Start here
  items:
    # FIRST ITEM OF THE FIRST GROUP, which is what makes this the first link in the sidebar on every
    # page: _layouts/default.html renders site.data.nav in file order, group by group, and takes the
    # first item of the first group as the first anchor in the nav element. Moving this entry down,
    # or adding a group above this one, silently demotes it -- the layout has no notion of a pinned
    # row, so position in this file is the whole of the mechanism.
    #
    # THIS ROW USED TO BE `The KORUS framework`, AND THE REASON FOR THAT EXPIRED RATHER THAN BEING
    # OVERRULED. That page was pinned first because it was the only page on the site about the
    # method, while the landing page sold the PowerShell tooling. On 2026-08-16 the landing page
    # became the method's own overview, so the framework essay is now the account BEHIND the
    # overview rather than the only route to it, and it sits third -- after the two pages a new
    # reader needs first.
    - label: Overview
      title: KORUS
      path: /
    # SECOND, because the first question is whether to bother. Claude Code ships its own worktrees
    # now and they cover more than they used to, so the FAQ opens by saying which half of this
    # project a reader still needs -- and says plainly when the answer is "none of it".
    - label: FAQ
      title: Frequently asked questions
      path: /FAQ.html
    - label: Quickstart
      title: Quickstart
      path: /QUICKSTART.html
    - label: Run a KORUS build
      title: Run a KORUS build
      path: /KORUS-BUILD.html
    - label: The KORUS framework
      title: "The KORUS framework: Lessons learned the hard way"
      path: /KORUS.html
    - label: Concepts
      title: Concepts
      path: /CONCEPTS.html
    - label: Feed this to Claude Code
      title: Here's what to feed to Claude Code
      path: /FEED-THIS-TO-CLAUDE-CODE.html

- group: Set it up
  items:
    - label: Limits and requirements
      title: Limits and requirements
      path: /LIMITS.html
    - label: Desktop accounts
      title: "Several desktop instances, one per Claude account"
      path: /DESKTOP-ACCOUNTS.html
    # THIS WAS A RAW MARKDOWN LINK UNTIL 2026-08-16, and it was the worst defect on the site. The
    # file lived at the repository root, outside the Jekyll source, so the one page a reader opens
    # specifically to get started was served as unstyled plain text: literal `##`, `**bold**` and
    # `|table|pipes|`, full-width lines, no nav, and no way back except the browser's Back button.
    # Six independent reviews of the live site flagged it, and it was the only defect all six found.
    # Moving it under docs/ renders it through the same layout as every other page; the raw source
    # is still served beside it at /INSTALL.md, the way every other page's source is.
    - label: Installer reference
      title: Install
      path: /INSTALL.html

- group: Run a build
  items:
    - label: Running multiple sessions
      title: "Running multiple sessions"
      path: /RUNNING-MULTIPLE-SESSIONS.html
    - label: Worker brief
      title: "Brief a worker session"
      path: /WORKER-BRIEF.html
    - label: Worktrees
      title: Worktrees
      path: /WORKTREES.html
    - label: Coordination
      title: Coordination
      path: /COORDINATION.html
    - label: Steering
      title: Steering a running session
      path: /STEERING.html
    # THE "(design)" SUFFIX IS LOAD-BEARING AND IS NOT DECORATION. This page and Usage awareness both
    # say in their own opening that they ship no code, and both said so before 2026-08-16. What they
    # could not say is anything at all BEFORE the click: in the sidebar they sat identically to Hooks
    # and Worktrees, so a reader budgeted time for a feature, read the page, and found homework. Six
    # independent reviews of the live site raised it. The suffix is the only place the distinction
    # can reach a reader who has not opened the page yet.
    - label: Session mail (design)
      title: "Session mail for a KORUS build"
      path: /SESSION-MAIL.html
    - label: Sequence allocation
      title: Sequence allocation
      path: /SEQUENCE-ALLOC.html
    - label: PRs and merges
      title: "Landing work: PRs and merges"
      path: /PR-AND-MERGE.html
    - label: Pruning
      title: Pruning worktrees
      path: /PRUNING.html

- group: Guardrails
  items:
    - label: Hooks
      title: Hooks
      path: /HOOKS.html
    - label: Leak gate
      title: The leak gate
      path: /LEAK-GATE.html
    - label: Usage awareness (design)
      title: "Usage awareness: Knowing when to stop, without lying about it"
      path: /USAGE-AWARENESS.html
    - label: CI for leaders
      title: "CI for leaders: What done means when the author cannot vouch for the change"
      path: /CI-FOR-LEADERS.html
    # Under Guardrails rather than Run a build because it is a control that runs unattended, like the
    # hooks above it. A reader arrives at it from "what watches CI", not from "how do I start a
    # session".
    - label: Watching for a red
      title: Noticing a red without a session watching for it
      path: /CI-RED-WATCH.html

- group: Reference
  items:
    - label: Troubleshooting
      title: Troubleshooting
      path: /TROUBLESHOOTING.html
    - label: Every script
      title: "Every script, and what it does"
      path: /SCRIPTS.html
    - label: Token accounting
      title: "Token accounting: What a usage meter measures, and what a plan buys"
      path: /TOKEN-ACCOUNTING.html
    - label: Tips and tricks
      title: Tips and tricks
      path: /TIPS-AND-TRICKS.html
    # Under Reference because a reader arrives at it to look up how long a playbook may be, not to
    # install anything. It ships no gate: its own opening says every length figure on it is a
    # judgment, because the 2026-09-04 sweep found no study that varies document length at all.
    - label: Playbook size
      title: "Playbook size: Nobody has measured where an instruction document stops being followed"
      path: /PLAYBOOK-SIZE.html
    # A STORED EXTERNAL STANDARD, which is why it sits under Reference rather than Guardrails. It
    # ships no gate and nothing checks a diagram against it. The page carries the owner's spec whole
    # and adds a measured contrast audit of the spec's own tokens, so a reader arrives here to look
    # something up rather than to install anything.
    - label: Flowchart standard
      title: Fluent 2 flowchart design standard
      path: /FLOWCHART-STANDARD.html

# Evaluations of third-party frameworks, one page each. The naming follows the CASE-STUDY-* series
# already in docs/: a shared prefix, then a kebab-case name, so a second evaluation is
# FRAMEWORK-<name>.md and sorts beside this one in docs/.
#
# THESE PAGES NO LONGER SHARE A FIXED SECTION ORDER. Through 2026-08-16 they did, gated as HS-21 in
# docs/HOUSE-STYLE.md; that rule is retired there, with the reasoning, once the BMAD page moved to a
# shorter reader-directed shape and left the series at one comparable member. FRAMEWORK-spec-kit.md
# still carries the old nine-section shape; nothing requires a new page to match it.
- group: Other frameworks
  items:
    - label: Spec Kit
      title: "Spec Kit 0.16.4 for a KORUS build"
      path: /FRAMEWORK-spec-kit.html
    - label: BMAD
      title: "BMAD 6.11.0 with Ultracode: A how-to"
      path: /FRAMEWORK-bmad.html

- group: Elsewhere
  items:
    # These documents lived here until the split and are now their own repository. The row is one
    # row on purpose: this site routes to them, it does not mirror them, and a second copy of the
    # index is the drift both projects are about.
    - label: Security standards
      title: Security standards for teams building with AI coding assistants
      url: https://secure-development-standards.pages.dev/
    - label: CLAUDE.md template
      title: A working agreement to copy into your own repository
      url: https://claude-multisession.pages.dev/CLAUDE.md.template
    - label: Source repository
      title: claude-multisession on GitHub
      url: https://github.com/wshallwshall/claude-multisession
