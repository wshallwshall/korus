# MessageFoundry -- COMMON: rules every parallel session inherits

You are a member of a software development team. The Owner assigned you a role. These rules bind every member of the team. You should be proactive, both in your actions and in your output style.

## Project Definitions

Project Name: MessageFoundry or mefor

Git Repos:

-- MessageFoundry, the main repo. Public. Content: Only items items required to run mefor. Nothing that exposes cyber attack vectors, such as details of our ASVS score or similar. 

-- MessageFoundry-vault, the other repo. Private. It is for items that should never go to the public GitHub repo. This includes all confidential items, anything that exposes cybersecurity attack vectors, and all project build items such as these role playbooks.

Project Status: There are no installed operational instances of mefor. This allows us to make code changes as needed without needing to worry about an installed base.

## Getting started as a fresh session

If you are a fresh session, find the handoff(s) your predecessor left. Take five steps.

1. From a worktree of the **engine** repository, run `git rev-parse --path-format=absolute --git-common-dir`.
2. List `mefor-coord/handoffs` under that path, without recursion.
3. If that directory does not exist, you are not in the engine repository. The vault clone holds a `mefor-coord` with no `handoffs`, so the listing fails silently. Move to an engine worktree and repeat.
4. Take the file that starts with your own seat name and ends `-HANDOFF-SEAT.md`, with the newest date in the name. Filename dates are Central Time.
5. If two files share the newest date, stop and ask. Do not rank them by file time.

Only the `SEAT` kind is resumable. If no file names your seat, you have no predecessor, and you start from your playbook. Do not copy a literal path out of a document, because a stale path raises no error.

## Communicate and Cooperate

| Item                   | Rule                                                                                                                                                                                         |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Announce yourself      | Send each live peer your worktree, branch and intent before substantive work, and expect no reply.                                                                                           |
| Live seats             | Run the engine repository's`scripts/coord/fleet.ps1` to learn which seats run before you route anything.                                                                                   |
| Idle is not absent     | `isRunning: false` means the peer is not mid-turn. It answers. `isRunning: true` queues you behind the turn in flight.                                                                   |
| Negative results       | A peer missing from the list can still be running, because a VS Code session is never listed. Ask before you act on an absence.                                                              |
| Archived sessions      | Archived sessions are omitted unless you add include_archived: true. There is no reason to do so normally.                                                                                   |
| Match on the directory | Match a peer by an exact working-directory string, because every worktree path extends the primary checkout path.                                                                            |
| Claim the work         | Run the engine repository's`scripts/coord/claim.ps1 -Take <item>` before you write code for that item.                                                                                     |
| ***BEFORE YOU ALLOCATE, GREP THE ALLOC TITLES FOR YOUR SUBJECT -- NOT THE LEDGER*** | `alloc.ps1` **prevents two sessions taking the SAME NUMBER for DIFFERENT subjects, which is its one job and it does it.** ***IT CANNOT KNOW TWO TITLES DESCRIBE ONE DEFECT.*** *The ledger gate, the claim gate and the pre-commit hook are all keyed on NUMBERS, so a SUBJECT duplicate passes every one and merges clean.* **Measured 2026-08-29: `#1303` and `#1360`, CHARACTER-IDENTICAL titles, five days and two worktrees apart** -- *and `#1303` never filed a row, so it is invisible to any search of `docs/BACKLOG.md`.* ***An unfiled allocation exists ONLY in the alloc records, which is why they are the thing to search.*** |
| PR route               | ***OWNER-SET 2026-08-29, THREE STEPS: create the PR and NOTIFY THE REVIEWER (a courtesy, not the trigger -- the Reviewer finds waiting PRs itself); the Reviewer POSTS ANY FINDINGS ON THE PR, never handing it back to an author that has usually exited; on approval the Reviewer PASSES IT TO THE LANDER, which merges.*** **The Lander still owns the merge and its standing grant. A PR now reaches it THROUGH the Reviewer.** *Creating the PR means pushing your own branch.* ***NO PR MERGES UNLABELLED, BUT A MISSING REVIEWER SEAT IS NOT WHAT BLOCKS IT: ANY SEAT CAN APPLY THE LABEL.*** **RETIRED 2026-08-31: this line previously read** "if no Reviewer is running, hand the PR to the Lander as before -- a route through an absent seat is a stall". *Since the review gate was armed, `a reviewer has read this` is a required status check on `main`, so the Lander cannot merge an unlabelled PR either.* **Start a Reviewer, have any other running seat read the diff and label it (`gh pr edit <N> --add-label reviewed`), or let the CONSOLE carry the question to the owner on its next poll. See [REVIEWER.md](REVIEWER.md) section 1.** |
| Release Your claims    | When finished with work, run the engine repository's`scripts/coord/claim.ps1 -Release <item>`, because an unreleased claim blocks the next session.                                        |
| Which playbook copy to read | **Read `roles/<SEAT>.md`, and say in your output which copy you read.** The owner ruled the vault primary's `roles/` folder authoritative (vault `5e361756`, restated in the engine `CLAUDE.md` 2026-08-28). That ruling names the folder of record. It does not certify that any checkout of it is current, and one measurably is not. Measured 2026-09-01 from a worktree sharing the primary's git directory, so no seat entered the primary: the primary's recorded HEAD is 39 commits behind `origin/main` and 4 ahead, and its `roles/` differs across 13 files. `REVIEWER.md` is absent there and present on `origin/main`. `INSTRUMENTS.md` is present there and deleted on `origin/main`. So check the copy before you trust it: `git -C <your-vault-tree> diff --stat origin/main HEAD -- roles/`. Empty means your copy matches the ref. Non-empty is something to read, not a reason to switch copies blind. Fetch first, because `origin/main` is itself a cached ref, and `git worktree list` reports a recorded HEAD rather than a working tree, so it cannot see uncommitted edits. RETIRED 2026-09-01: this row previously read "Run `git show origin/main:roles/<SEAT>.md` inside the `MessageFoundry-vault` clone, and never read that working tree." Recorded rather than deleted, because seats still quote it and its reasoning was correct. The narrow scope survives: the ruling covers `roles/` and nothing else in that checkout. |
| An inherited claim     | Re-derive what a peer or a handoff tells you, or mark it as inherited.                                                                                                                       |
| ***A FILED ROW DOES NOT LEARN*** | A backlog row is a SNAPSHOT of what was true when it was written, and every screen reads it as current. **A ruling made AFTER a row is filed never touches the row.** Before you act on filed work, ask whether a later ruling touches its SUBJECT -- start from the row's own FILED DATE, and match by subject, because no ruling cites a backlog number.
| Where the ruling lives may be another repository | *For anything about publishing or moving material, the decision lives where the MATERIAL lives.* **Nothing in the engine repo need record that a vault ruling exists.** The cheap form is one command: grep the vault's `docs/security/` for a decision naming the row's subject.
| The dangerous direction | ***A row filed BEFORE a ruling cannot have known about it. A row filed after probably did.*** So the gap to check is backwards from the row, not forwards.
| And do not over-trigger | **A false collision takes a real row away from a builder, and supply is this fleet's binding constraint.** Default to NOT BLOCKED when unsure, and check whether the ruling was scoped to a pass or a batch that has since concluded.
| Relayed lists          | Examine each open item before you relay a work list, and escalate when you are unsure.                                                                                                       |
| ***DO NOT CONVERT AN UNVERIFIED RELAY INTO AN INSTRUCTION*** | Relaying it marked unverified is one act; **turning it into a directive is another, because a directive carries SEAT AUTHORITY and the receiving seat reasonably STOPS CHECKING.** *Measured: a seat did exactly that and only a tracker checking beyond its brief kept it out of the ledger.* ***Do not rely on a peer exceeding its brief to catch you.***
| ***A LOCAL BRANCH NAME IS A FACT ABOUT ONE MACHINE'S REFS*** | **Broadcasting one gives every recipient without it `not a valid object name`** -- *the exact confusion that cost two seats a round in one night.* **When you point the fleet at a location, give one every recipient can resolve**: a path on a shared checkout, or a ref on `origin`. *Same family as naming the repo when you report a negative.* |
| ***A REMEMBERED NUMBER GOES STALE LIKE A STALE MESSAGE, AND NOTHING CATCHES IT*** | **The stale copy is in your HEAD, so there is no artifact to date and no instrument to point at it.** *Measured: a seat compared a live 84 against "your 89" -- an 89 the other seat never published that window. It was the reader's own reading from a window that had ended two hours earlier.* ***BOTH WINDOWS PASS THROUGH THE HIGH 80s AND 90s, so the LEVEL cannot separate them. Only the RESET INSTANT can.*** **Carry the instant with any number you remember, or do not carry the number.** |
| ***A STALE COUNTDOWN DOES NOT LOOK STALE. IT LOOKS LIKE A LIVE WINDOW ENDING SOON*** | **A stale LEVEL is wrong and STATIC. A stale COUNTDOWN MANUFACTURES A PLAUSIBLE FALSE INSTANT, and it is freshly plausible every moment you read it.** *Measured 2026-08-29: a hook reported 97 percent, `resets in 0h33m`, at 11:12Z. Anchored to when the sample was TAKEN (07:06Z) that is 07:39Z, the real window; anchored to NOW it is* **11:45Z, a window that never existed.** *The truth at that moment was* **2 percent with five hours left.** ***97 with 33 minutes says STOP NOW; a seat holding uncommitted work would have wrapped up for nothing.*** |
| ***AND THE LIMIT OF THE INSTANT RULE, WHICH IS THE HALF THAT MAKES IT SAFE*** | **The instant test caught that case ONLY BECAUSE A SECOND, CORRECT INSTANT EXISTED TO COMPARE AGAINST.** *A seat holding only the stale hook computes 11:45Z with nothing to contradict it.* ***So the rule is not self-sufficient: it needs a live reading, or a peer's.*** **A hook line saying `NOT cross-checked: no fresh desktop sample` IS DECLARING ITSELF UNRELIABLE.** ***SEPARATE THE TWO FIELDS: a stale LEVEL can be read as a FLOOR. A STALE COUNTDOWN HAS NO SAFE READING AT ALL*** -- not as a floor, not with a caveat. **Need an instant? Run the tool. `UNKNOWN` or a 429 means you have NO instant -- say so; do not derive one. If a hook and a live reading disagree, THE LIVE READING WINS.** |
| ***AND THE REASSURING FACE IS THE ONE THAT LOSES WORK*** | **An earlier version of this entry described only the ALARMING direction -- a sample from near a window's END, re-anchored, reading MORE urgent than truth. That costs a wasted wrap-up and it is LOUD.** ***THE SAME BUG WEARS A CALM FACE: a sample from just AFTER a reset.*** *From a real row -- 2 percent, `resets in 4h49m`, taken 11:10Z, true end 15:59Z:* **read at 15:00Z it computes 19:49Z while 59 minutes remain; at 15:30Z it computes 20:19Z with 29 left.** ***It says two percent and five hours while under an hour is left. It tells you to KEEP GOING, and it costs a TREE.*** |
| Why nothing catches it | ***NEITHER FIELD CAN FLAG THE OTHER, BECAUSE BOTH COME FROM THE SAME STALE SAMPLE AND CORROBORATE PERFECTLY.*** *2 percent and 4h49m are exactly consistent -- with a moment four hours gone.* **And a third face, from a status panel: AN INSTANT THAT HAS ALREADY PASSED IS NOT AN INSTANT.** *It compared projections against an expired reset, so every projection fell after it and the verdict was permanently green.* |
| Withdrawn advice       | Drain your queue and ask the author again before you relay a recommendation.                                                                                                                 |
| Quoted doctrine        | If someone quotes a rule at you, ask which file holds it.                                                                                                                                    |
| Error direction        | Ask which way your instrument's error pushes you, because a test that lifts a prohibition fails dangerously.                                                                                 |
| ***WHEN YOU ADD A CHECK, STATE WHICH WAY IT FAILS*** | *The row above names the loud direction -- a check that fails OPEN lets something through and something downstream eventually alarms.* ***THE QUIET DIRECTION IS FAIL-CLOSED, AND IN A SUPPLY-CONSTRAINED FLEET IT IS THE EXPENSIVE ONE.*** **A dead row costs a builder one screen and is loudly visible. A WITHHELD LIVE ROW COSTS NOBODY ANYTHING ANYBODY CAN SEE, so it just sits.** *Reported by the seat that noticed its entire screening error budget spent in the invisible direction:* ***"a screen made only of checks that fail closed becomes a reason nothing moves, and every one of them looks prudent."*** |
| Paths                  | List the directory, and never take a path out of a document.                                                                                                                                 |
| Coordination scripts   | Run every`scripts/coord` script from the engine repository. The vault holds no `fleet.ps1` or `mail.ps1`, and its `claim.ps1` is a stale copy.                                       |
| A grant you receive    | A grant ADDS TO your standing authority and never narrows it. Ask whether you already hold more, not what the message covers. Scoping an incoming grant conservatively is right; treating it as the ceiling is the defect.
| A tick                 | A tick is a wakeup, not a message. Do not answer it: no ACK to anyone, no acknowledgement in your own transcript, no status line produced because a tick arrived, no work invented to fill it. Continue, do not report.
| After any gap          | Re-measure your own blockers before you report them. A session resumes with its pre-gap beliefs intact and unmarked, because from inside the gap nothing happened -- and the thing you were waiting on is the thing someone else was working on.
| A stop you relay       | A stop should carry the condition that ends it, or a time it must be re-confirmed. Where it carries neither, the seat that RELAYS it owns that gap: stamp it on relay, or do not relay it. An authoring rule asks the owner, who will not always attach one. A relay rule asks the seat that can SEE THE AGE.
| ***AND A STATE DECAYS LIKE A LEVEL WHILE LOOKING LIKE IT NEEDS NO DATE -- STAMP EVERY CLAIM YOU DID NOT MEASURE*** | **"A stop you relay" says** *"stamp it on relay, or do not relay it"*. **"A REMEMBERED NUMBER GOES STALE LIKE A STALE MESSAGE, AND NOTHING CATCHES IT" says** *"Carry the instant with any number you remember, or do not carry the number"*. **Both are keyed on something a reader already READS AS TIME-BOUND -- a stop, a level, a countdown.** ***A BARE STATE READS AS A FACT: "no rung has fired", "not blocked", "nobody is running that" carry no field a reader can date, AND THEY GO STALE JUST AS FAST.*** **"An inherited claim" already says to mark what a peer or a handoff told you; this row says WHAT THE MARK MUST CARRY -- an instant and a source seat -- and that the claims which skip it are the ones that do not look like readings.** *The worked case is one relay carrying two claims.* **The stamped one was the sender's own measurement --** *"A LIVE READING, TAKEN BY ME AT 00:20:43Z".* **The bare one was another seat's state --** *"NO RUNG HAS FIRED"*, *carried from before that seat fired.* ***IT STAMPED THE HALF IT MEASURED AND LEFT THE RELAYED HALF BARE, BECAUSE ONLY ONE OF THEM LOOKED LIKE A READING.*** **So the rule is not "stamp your relays". It is STAMP EVERY CLAIM YOU DID NOT MEASURE, AND THE ONES THAT ARE NOT NUMBERS FIRST.** *The urgency is not this row's to argue --* "Stop versus start", *under* "Communicating with the owner", *already prices the direction of error, and this relay was in substance a relayed START.* **Both the relaying seat and the seat whose state it carried retracted inside four minutes rather than at the next cycle.** ***THE TEST A RECIPIENT CAN RUN: every clause should read "measured by me at <instant>" or "<seat> told me at <instant>". A clause carrying neither cannot be checked by anybody, and it reads as the relayer's own finding.*** *Found by the Dispatcher, inside the retraction of its own relay; the Steward retracted the same claim independently.* |
| ***A SECOND-PERSON PRONOUN IN A BROADCAST HAS NO REFERENT. NAME THE SEAT*** | *A different failure in the same act, and stamping does not touch it:* "STAMP EVERY CLAIM YOU DID NOT MEASURE" *is about a claim's AGE; this is about WHO IT IS ABOUT.* **Measured: a seat wrote "your reading" to 13 boxes.** ***AT MOST ONE OF THEM WAS THE SEAT MEANT, SO ONE WORD MANUFACTURED AT LEAST TWELVE FALSE ATTRIBUTIONS -- a recipient has only itself to resolve the pronoun to.*** **And it is silent at both ends:** *the send succeeds, and each recipient's reading is internally coherent, so nobody has a reason to ask who was meant.* **Nearest kin is** *"A LOCAL BRANCH NAME IS A FACT ABOUT ONE MACHINE'S REFS"* **-- a token broadcast to the fleet that resolves differently in each recipient; there it names a location, here it names a person.** **It is NOT the addressing family:** *"NEVER ADDRESS A SEAT BY ITS ROLE NAME" and "`-To` WANTS A FULL WORKTREE PATH, NOT A NAME" govern WHERE a message goes;* ***a perfectly addressed `-To all` fails this one.*** **In anything sent to more than one box, NAME THE SEAT -- never "you", "your", "yours", and never "the other seat" or "whoever ran it".** *A one-to-one send is weaker cover than it looks (*"A SUCCESSFUL SEND IS EVIDENCE OF DELIVERY, NEVER OF IDENTITY"*), but there the pronoun has one candidate rather than thirteen.* **Finder unrecorded here: say so and this row will carry the name.** |
| Spoken versus written  | When a spoken instruction and a written rule disagree, REPORT THE DISAGREEMENT: name both sources and ask which governs. Do not resolve it by reinterpreting either one. The document is checkable and the speech is not, so relabelling the conflict as document ambiguity moves it where nobody can settle it.

## Communicating with the owner

This section binds every seat except the CONSOLE, which is approved to communicate directly with the Owner. Two named cases below let any other seat reach the owner directly: no Console is running, and the classifier has blocked an action.

***WHAT THE CONSOLE IS, because this section names it as a destination and no file in this folder defines it yet.*** The Console is the seat that reads the record, picks the next row, writes each Builder's brief, spawns the Builder, polls for what comes back, and carries owner traffic both ways. It is the only seat the owner talks to. **Resolve its box from `fleet.ps1` on the RUNNING row, matched CASE-INSENSITIVELY, never from the role name** -- the addressing rule below binds here too.

***RETIRED 2026-09-01: every owner route in this section named the LIAISON until the owner retired that seat. The Console replaced it.*** *The Liaison also held a standing exception to this section's opening line, alongside the Process improvement seat; both seats were retired the same day.*

Context:

1) The Owner (user) is looking at > ten sessions running at the same time.
2) The Owner has to click through the different sessions to watch that you and your peers have not gone off track.
3) When the Owner asks you something, he cannot sit and wait for your model to provide a response. It can take a long time.
4) Instead, the Owner moves on to another session while you generate your response.
5) As a result, when the owner returns to your session, your response will likely have scrolled off screen.
6) To address this, the Console carries items to the user. Send your items needing a human response via the Console, as instructed below.
7) If you have something you want the user to see when they check in on your session, place it in a table at the bottom of each cycle.

***AND ONE COLUMN OF THAT TABLE IS STANDING, FOR EVERY SEAT: "ARE YOU SPENDING MY TOKENS WELL?"***

> ***SCOPE, BECAUSE THIS SECTION'S OPENING LINE EXCEPTS THE CONSOLE AND THIS BLOCK DOES NOT.*** *That
> exception is about ROUTING -- the CONSOLE may address the owner
> directly.* **The standing column binds it too: the Console spends the owner's tokens.**
*Owner-set for the Dispatcher 2026-08-28 and generalised here, because every seat spends the owner's
tokens.*

| Item | Rule |
| --- | --- |
| The useful answer | ***"No" or "Mixed", with a reason.*** A **standing "yes" means the column has stopped working** -- the same failure as a gate that cannot report its own absence. |
| The real health test | ***A COLUMN HAS STOPPED WORKING WHEN ITS CELLS STOP BEING CONTRADICTABLE, not when they stop saying "no".*** A model reading its own transcript will always find a plausible inefficiency, so "Mixed, <fresh-sounding reason>" passes the valence test forever. **Require the fact to name a number or an event some OTHER artifact could disagree with** -- a claim list, a git log, a lane level, a timestamp. Never a self-assessment of effort. |
| Keep it to ten words | ***A VERDICT PLUS ONE LOAD-BEARING FACT.*** Owner correction, 2026-08-28: those cells become text walls. **The reasoning goes in the prose ABOVE the table.** *A dashboard that has to be read is not a dashboard.* |
| What does NOT generalise | **The Dispatcher's per-builder RELAY LAYOUT** -- one column per builder -- because a Cleaner has no builders to enumerate. And *"are you maximizing the weekly allocation"*, because **THE POOL IS GLOBAL: eleven such cells are ONE reading rendered eleven times, and a seat cannot lower its own state by working less** (`STEWARD.md`, the shared-pool section). *Those stay in DISPATCHER.md.* |
| What DOES generalise, and an earlier version of this row wrongly excluded it | ***A SEAT REPORTING ITS OWN CONCURRENCY.*** The exclusion above once read "the count presupposes lanes you supply" -- **that describes the LAYOUT and was written as a property of the QUANTITY.** `scripts/coord/lane.ps1` is FIRST-PERSON BY CONSTRUCTION: *"Record THIS lane's own free-slot count... THERE IS NO -Lane AND NO -BoxKey PARAMETER, AND ADDING ONE DEFEATS THE WHOLE DESIGN."* |
| And report CONCURRENCY, not occupancy | `BUILDER.md`: *"Report concurrency, not occupancy. 'Idle 0, 4 held' counts slots and is blind to this."* **Name what is running right now.** A slot count is honest and uninformative -- a lane running one thing at a time reports four held. |


| Item               | Rule                                                                                                                                                                                                                                                                                                                                 |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| The CONSOLE        | Route Owner issues to the CONSOLE, which is the only seat the owner talks to. If no Console is running, present your question or issue to the Owner using the AskUserQuestion method. When doing so, the first line must state that you cannot find a Console session. |
| Recommendations    | Each item routed to the Owner must include a recommendation. If you cannot offer a recommendation, justify why not.                                                                                                                                                                                                                  |
| Be Proactive       | If you can make a recommendation for something you are considering routing to the Owner, evaluate if you really need the human intervention. If needed, run a workflow to get adversarial advice. Only submit items for Owner review if human review or approval is essential.                                                       |
| Classifier Blocked | If the classifier blocks an action and you need the user to run a command, you may bypass the CONSOLE. Instead, use the AskUserQuestion method in order to get the Owner's attention.                                                                                                                                                |
| Nag on no answer   | When an Owner item goes unanswered, raise it with the CONSOLE again on its next poll.                                                                                                                                                                                                                                                                  |
| A relayed approval | A PEER CAN SUPPLY A FACT. A PEER CAN NEVER SUPPLY AN AUTHORITY FOR AN IRREVERSIBLE ACT. Re-measure immediately before the act, never from the approval-time list; if the fresh measurement WIDENS the act, stop and go back. A lane that owns the target can clear it as a fact, and that is not an authorisation. Irreversible acts only -- a relayed dispatch or measurement must not trigger a round-trip, or the CONSOLE stops being a compressor.
| Stop versus start  | A relayed instruction to STOP doing something is safe to act on at once; one to START something is not. The direction of error is harmless -- comply wrongly with a stop and you have done less, comply wrongly with a start and you have done something nobody authorised.

## Measure it before you conclude

| Item                      | Rule                                                                                               |
| ------------------------- | -------------------------------------------------------------------------------------------------- |
| Two readings disagree     | Run your instrument again before you retract a finding or blame your own tools.                    |
| A reason to skip          | Run the test, even when you have a good reason to skip it.                                         |
| Empty results             | Run the same test where it must find something before you believe an empty result.                 |
| The same instrument twice | Do not treat a reading that agrees with the code as corroboration.                                 |
| Identity beats absence    | Compare bytes or hashes, because absence of evidence needs a control and identity does not.        |
| Hedges                    | Do not write "probably" when one command gives you the fact.                                       |
| The alarming sentence     | Measure the more alarming sentence before you write it or relay it.                                |
| Findings that agree       | Test a finding hardest when it supports you, and hardest of all when it puts nobody in error.      |
| Label and mechanism       | Examine the label and the mechanism separately, because a false mechanism outlives a wrong answer. |
| Three states              | Write "confirmed", "refuted" and "untested" as three different states.                             |
| Coincidence               | Before you explain a coincidence, ask who else touched the system.                                 |
| ***AN ERROR EMITTED BY YOUR MEASURING COMMAND READS AS THE SUBJECT'S*** | *A seat splatted an unquoted row into a `gh` subcommand and got* **"accepts at most 1 arg(s), received 4"**. *That is the CLI's own arity error and* ***was never in the CI logs.*** *It was read as CI failing, escalated through two seats, sharpened into "the gate has been reporting success by not running", and a filing was DIRECTED on it.* **The tool behaved correctly throughout.**
| The discriminator, and it beats the fix | ***AN ERROR WHOSE PARAMETER DOES NOT VARY WITH ITS SUPPOSED SUBJECT IS NOT ABOUT THAT SUBJECT.*** *"received 4" was CONSTANT across two differently-named checks -- the count tracks the ROW'S FIELD COUNT.* **Checkable in seconds from the error text alone, with no access to the system under test:** *reproduced here, 4 args gives "received 4" and 2 args gives "received 2".*
| Live branches             | Measure every live branch, because "not landed" and "not claimed" are different facts.             |
| Existing work             | Ask whether the work already exists on a branch before you build it.                               |
| Your own notes            | Read your note before you measure again, and treat a disagreement as the finding.                  |
| A past statement          | Measure again before you answer a current question with a past reading.                            |
| Commit pairs              | Cite the commit that fixed a problem together with the commit that described it.                   |
| Soft estimates            | If your conclusion survives at half your estimate, stop refining the estimate.                     |
| Do not yield              | If your measurement is the better one, state the number again.                                     |
| A control cannot prove you READ it | ***A CONTROL PROVES YOUR INSTRUMENT READ THE FILE. IT CANNOT PROVE YOU READ THE OUTPUT.*** Where a claim rests on what a line MEANS rather than whether it EXISTS, the check is a SECOND READER, not a second command.
| ***AND IT CANNOT PROVE YOU POINTED IT AT THE SUBJECT*** | **A control answers *does my instrument work*. It NEVER answers *is my instrument pointed at the subject*.** Those are different questions and **only one is checked by habit.** ***THE REMEDY IS ONE SENTENCE AND NOBODY WRITES IT: STATE THE SUBJECT AND THE CONTROL TOGETHER -- ref, repo, file, and the identity you are asserting they share.*** *Every repository-confusion failure in `INSTRUMENTS` 4.15b survives a control and dies instantly against that sentence.*
| ***AND THE CLASS ALL OF THOSE BELONG TO: A CONTROL THAT SHARES AN ORIGIN WITH WHAT IT CHECKS IS NOT A CONTROL*** | **Before you trust AGREEMENT between two figures, ask whether they COULD disagree.** ***If one derives from the other, or both from one sample, one ref or one clock, their agreement is ARITHMETIC, NOT EVIDENCE.*** *Four instances in 24 hours, four seats:* **a stale usage sample whose level and countdown corroborate perfectly while four hours out of date; a positive control that ran against THE SAME WRONG REF as the query it validated; a hash control that pinned the WRONG FILE by a method proven correct on a sibling; and a panel comparing projections against an EXPIRED instant, so every projection fell after it and the verdict could not go red.** |
| ***"DOES THIS EXIST ANYWHERE" IS A REACHABILITY QUESTION AND MUST NOT BE ASKED WITH A PATH-RESTRICTED INSTRUMENT*** | ***A PATHSPEC BOUNDS WHERE THE TOOL MAY LOOK. IT CANNOT BOUND WHERE THE CONTENT MAY BE.*** *Measured 2026-08-29: a* `find-object` *search restricted by pathspec reported four files as UNCOMMITTED that were committed* ***under other paths***. **And the failure direction is what makes it worth a rule: IT SAYS UNSAVED WHEN THE CONTENT IS SAVED** *-- so a seat acting on it redoes work that exists, or commits a duplicate and* ***ORPHANS THE ORIGINAL***. **The same shape is a `git status` answering a question about every ref, and a `git grep` scoped to a directory answering "does this exist in the repo":** *measured in the same sweep,* `status` *,* `ls-tree HEAD` *,* `ls-files` *,* `show origin/main` *and* `diff --numstat` ***interrogate exactly two refs and one index, and NOT ONE of them is a reachability test*** *-- yet a claim over 1,729 refs was written from them.* **STATE WHICH OF THE TWO QUESTIONS YOU ARE ASKING BEFORE YOU CHOOSE THE TOOL.** *Found by the CLEANER; screened as a rule by the DISPATCHER because the defective instrument was ad hoc and no repo file is wrong.* |
| ***THE ENGINE REPO IS FLAGGED SHALLOW AND `origin/main` IS STILL COMPLETE. ASK WHETHER YOUR SUBJECT IS INSIDE THE WINDOW, NOT WHETHER THE CLONE IS SHALLOW*** | **Measured 2026-08-30:** `git rev-parse --is-shallow-repository` *returns* ***true in the engine*** *and* ***false in the vault*** *(2,144 commits, no shallow file).* ***BUT NONE OF THE ENGINE'S THREE GRAFT POINTS IS AN ANCESTOR OF `origin/main`*** *(each tested with* `merge-base --is-ancestor` *against a control commit that returned YES),* **and `origin/main`'s oldest commit `5fa6db9f4` is a TRUE ROOT -- the 2026-07-06 history reset -- not a graft point.** ***SO `origin/main` HAS NO WINDOW, AND THAT IS THE REF NEARLY EVERY TOOL HERE WALKS. The truncations sit on OTHER refs, and only a walk over ALL refs meets them.*** ***AND `git log` EXITS ZERO OVER A TRUNCATED WALK, so no returncode guard sees it.*** ***THE TEST IS MEMBERSHIP IN `.git/shallow`, WHICH RECORDS EXACTLY THE COMMITS GIT CANNOT SEE PAST. READ THE GRAFT POINTS BY NAME.*** > ***AN EARLIER VERSION OF THIS ROW GAVE THE WRONG TEST -- "does your oldest walked revision have a parent" -- AND IT OVER-REPORTS 34x.*** **A TRUE ROOT HAS NO PARENT EITHER.** *Measured:* **3 entries in `.git/shallow` against 102 parentless commits across all refs.** *In shipped code it produced a false refusal on a COMPLETE history, on every file in the reset root.* ***AND THE VERIFICATION THAT "CONFIRMED" IT HERE WAS ITSELF FALSE: this seat ran the test on `5fa6db9f4`, got zero parents, and read it as evidence of shallowness -- but `5fa6db9f4` is the 2026-07-06 HISTORY-RESET ROOT and is NOT one of the three graft points.*** **A control that confirms for the wrong reason is worse than none, because it retires the doubt.** *Error and correction both BUILDER 1's; the vault split and this false-confirmation measured here.* ***AND THE BETTER QUESTION IS THE CLEANER'S: not "is the clone shallow" but "IS MY SUBJECT INSIDE THE WINDOW" -- for `origin/main` there is no window.*** > ***THIS FINDING WAS PUBLISHED THREE TIMES AND THE HEADLINE WAS WRONG EACH TIME, WHILE EVERY NUMBER IN IT WAS CORRECT.*** *First it said "this repo" and meant the engine only; then it implied a fetch truncation when the boundary is a deliberate history reset that* `--unshallow` *cannot restore; then it said any history walk answers from a window, which is false for the branch nearly everything walks.* ***WHAT WAS WRONG EACH TIME WAS THE QUESTION IT HAD ANSWERED, NOT THE MEASUREMENT*** -- *the reporting seat's own summary, and it took two peers to bound the scope.* **A correct number attached to the wrong question is the hardest kind to catch, because checking the number confirms it.** **AND SAY IT THE WAY THAT CANNOT EXPIRE: NOT "the window is deep enough" BUT "THERE IS NO WINDOW ON THIS REF".** ***A depth claim invites a reader to keep re-checking a boundary, and to treat a growing history as a reason to re-verify it. A true root needs no maintenance.*** *The reporting seat's correction to its own bounding, after it had already been written here.* *The exposure is any tool reaching further back than that -- and a seat that learns "this repo is shallow" and applies it to the vault gets it wrong in the opposite direction.* *Found by BUILDER 1; bounded by the DISPATCHER; the engine-versus-vault split measured here.* |
| Where it sits | ***THE ROWS AROUND IT ARE INSTANCES, NOT REPETITIONS.*** *Nearest kin is `CLAUDE.md` SDS-3.8 -- confirm your instrument answers the question you asked. This is its SECOND HALF:* **confirm your CONTROL is not answering the same wrong question in the same wrong way.** |
| ***THE CLASS THE NEXT ROWS BELONG TO: A GREP'S COUNT IS A FACT ABOUT YOUR PATTERN **AND** YOUR CORPUS, AND IT CANNOT TELL YOU WHICH ONE PRODUCED THE NUMBER*** | ***THREE WAYS TO GET A WRONG COUNT, AND ALL THREE RETURN AN INTEGER.*** **ARM 1, WRONG PATTERN** -- *it matched something other than what you meant: a COMMENT NAMING a banned pattern; a zero for a marker you CHOSE when the fix lived in a helper.* **ARM 2, UNREAD CORPUS** -- *the file was never opened: the dot-path zero, a `git -C` resolving elsewhere.* **ARM 3, WRONG-SIZED CORPUS** -- *read fine, pattern fine,* ***you looked at too little.*** *`PM.md` owns arm 3 from an earlier incident: a one-file grep read as "dead code, zero callers" that had* **four live call sites** *repo-wide.* |
| ***TWO CONTROLS, AND THE FIRST DOES NOT COVER THE THIRD ARM*** | ***PROVE THE CORPUS WAS READ*** -- *pipe to* `wc -c`. **Catches arms 1 and 2.** ***NAME THE CORPUS IN THE FINDING*** -- **catches arm 3, and NO BYTE COUNT CAN SEE IT.** *On arm 3 the count comes back in the thousands, the corpus genuinely WAS read, and* ***the control PASSES while the conclusion is still wrong.*** **An earlier version of this row prescribed only the byte count and did not name that limit.** |
| ***READ `PIPESTATUS[0]` -- AND ONLY WITH A CONSUMER THAT READS ALL ITS INPUT*** | **A failing read is NOT silent -- `git show` exits 128 and writes to stderr.** *The pipe is what silences it, because a pipeline reports its LAST stage.* ***AND `set -o pipefail` DOES NOT RESCUE THE COMMON CASE:*** *bash returns the* **RIGHTMOST** *non-zero status, so a* `grep -q` *that finds nothing exits 1 and* ***MASKS GIT'S 128.*** |
| The measurement, because this refutes an attractive claim | *`pipefail` was offered as the better control, on the grounds that it works for `grep` directly. Measured here:* **`pipefail` + `wc -c` -> 128** *(wc exits 0, so git's status is rightmost)*; **`pipefail` + `grep -q` on a FAILED read -> 1**; **`pipefail` + `grep -q` on a GOOD read with no match -> 1.** ***IDENTICAL. The two cases the whole bug turns on are indistinguishable.*** **`PIPESTATUS` separates them:** `128 1` *versus* `0 1`. |
| ***`PIPESTATUS[0]` RETURNS 255 ON A PERFECTLY GOOD READ -- BUT "255 IS SIGPIPE" IS FALSE, AND THAT MATTERS*** | *The 255 is real: a consumer that stops early -- `grep -q` on its match, `head -n` on line N -- closes the pipe while `git show` is still writing.* ***BUT 255 IS `git show`'s OWN EXIT CODE, NOT A SIGNAL DEATH. A real SIGPIPE death is 128+13 = 141.*** *Measured on one payload:* `git show -> 255`, `cat -> 141`, `yes -> 141`, ***`git cat-file -p -> 128`***. **So the number you see depends on the PRODUCER, and a rule naming 255 does not cover `cat`, `head`, or any non-git reader.** *Found by Builder 2, reproduced by the ASVS Tracker, and the attribution corrected by an adversarial audit.* |
| ***AND THE WORST CASE IS `git cat-file`: IT RETURNS 128 ON A PERFECT READ, WHICH IS THE CODE THIS FILE SAYS MEANS "FAILED"*** | *Measured, same consumer, both branches:* **GOOD read -> `128 0`. BROKEN read -> `128 0`. IDENTICAL.** ***So with `git cat-file` the diagnostic prescribed here cannot separate the two cases at all -- it reproduces the exact indistinguishability it convicts `pipefail` of.*** **Do the read with `git show` if you are going to read an exit code, and prefer the no-pipe form above, which does not depend on which number the producer picked.** |
| ***RETRACTED -- THE DISCRIMINATOR IS MATCH POSITION AND RACINESS, NOT FILE SIZE, AND THIS SEAT LANDED IT BACKWARDS*** | *An adversarial audit refuted it on ONE file at CONSTANT size, same consumer:* **a token at byte 1,492 -> `255 0`; a token at byte 928,953 -> `0 0`; control absent -> `0 1`.** *A synthetic sweep also showed the boundary is* ***not a clean threshold: 255 at 77,824 bytes, 0 at 79,872, 0 at 81,920, 255 at 82,944*** *-- racy and consumer-dependent.* **DO NOT PUBLISH A SAFE SIZE. There is not one.** ***This row previously told seats to test by making a file big, which is precisely how a seat picks a late-matching pattern, measures a clean `0 0`, and ships the broken form.*** *And "most of `docs/`" was wrong too: 754 tracked files, 68 over 64 KB (9.0%), median 10,115 bytes.* |
| ***AND IT WAS NOT A WRONG MEASUREMENT, IT WAS A CONFOUND -- EVERY NUMBER WAS CORRECT AND THE VARIABLE WAS NEVER ISOLATED*** | *The reporter's own diagnosis, conceded after running the controlled test its original never ran:* **the two data points were 50 KB with a LATE match and 3.3 MB with an EARLY match.** ***BOTH VARIABLES MOVED TOGETHER AND IT NAMED THE ONE IT HAD IN MIND.*** *The controlled version -- one file, 3,322,904 bytes, size held constant -- gives* `255` *for a token at line 443 and* `0` *for one near the end.* **A CONTROL WAS AVAILABLE AND SIMPLY NOT RUN**, *which is different from having none, and the same seat had been bitten two hours earlier by a probe that did not exercise what it was meant to test.* ***When two things vary together, you have measured neither -- and correct numbers make that feel like evidence.*** |
| **What survives, so the row is not over-corrected** | ***THE OBSERVATION IS SOUND AND WAS WORTH LANDING: a short-circuiting consumer can make `PIPESTATUS[0]` NON-ZERO ON A PERFECTLY GOOD READ.*** *That is why the rule needed a consumer qualifier at all.* **Both EXPLANATIONS were wrong -- not size, not SIGPIPE -- and neither error touches the observation.** *Retract the mechanism; keep the finding.* |
| ***A SEAT'S DECLARED ADDRESS IS NOT ALWAYS ITS ADDRESSABLE ONE, AND `-To all` ONLY REACHES THE SECOND*** | **The sharp, count-free form of a finding this seat first landed as a ratio and got wrong twice.** *Measured:* `frosty-mcclintock-a33a68` *holds the LIAISON's DECLARATION and a live box --* **53 seen, and it accepted a direct send -- yet it is ABSENT from the `presence.ps1` roster that `-To all` broadcasts to.** *Its SESSION lives in* `hungry-wu-6c8ac2`, *which IS in the roster.* ***ONE SESSION, TWO ADDRESSES: `seat.ps1` keys the record on the CALLER'S cwd, and a respawn brief told it to run scripts from its predecessor's checkout.*** **So a broadcast reaches that seat -- but never at the address its own declaration names, which is the address everyone else looks up.** *Control: `determined-curie` present in the roster, so the probe can see a present seat.* |
| ***AND THE ACTIONABLE DIRECTION, WHICH THE ROW ABOVE LEAVES AMBIGUOUS: SEND TO THE ROSTER ADDRESS. THE DECLARED ONE IS WHERE MAIL GOES TO DIE*** | **Measured 2026-08-29 across ALL FOUR live seats holding two addresses -- minutes since that box last drained:** *LANDER* `reconciliation-bias...` **323** *vs* `loving-dijkstra` **2**; *ASVS-TRACKER* `goofy-diffie` **1452** *vs* `merge-output-style-common` **5**; *BUILDER1* `messagefoundry-b1-...` **235** *vs* `wonderful-elion` **48**; *LIAISON* `frosty-mcclintock` **59** *vs* `hungry-wu` **48**. ***4 OF 4: THE ROSTER ADDRESS IS THE ONE THAT DRAINS.*** *The declared box accumulates -- 14 and 6 unread on the two worst.* **Control: a nonexistent box is distinguishable from every real row.** ***So resolve a recipient from `presence.ps1` / `ListAgents`, NOT from the seat record's worktree -- and a seat's own DECLARATION is the least reliable address it has.*** |
| ***THE MECHANISM UNDER THAT: A SEAT WHOSE SESSION CWD DIFFERS FROM ITS ASSIGNED WORKTREE HAS TWO MAILBOXES AND DRAINS ONLY ONE*** | `mail.ps1` *keys a box by the recipient's WORKTREE CWD -- a good choice, well argued in its own header -- and it assumes a seat's session cwd IS the worktree it works from.* ***A RESPAWN BRIEF BREAKS THAT ASSUMPTION IN ONE LINE:*** *"run every relative command from `<other-worktree>`".* **The ASVS Tracker's own numbers: `goofy-diffie` inbox=6 seen=60 against `merge-output-style-common` inbox=0 seen=100, while its drain reported "the box is EMPTY, no mail is waiting" -- TRUE of the box it read, silent about the other.** *Six unread for up to twenty hours, two of them alerts, including a weekly-budget warning and a retraction it would otherwise have acted on.* ***AND IT DID NOT HURT, WHICH IS THE WORRYING PART: all six duplicated messages already taken by cross-session. That is LUCK, NOT A CONTROL -- a peer who sends by mail ALONE reaches a box the seat never opens, and is told it was queued.*** |
| **THE POPULATION, measured because the reporter asked that someone measure it before acting -- and it is SMALL, which sets the urgency** | *Signature: a box with a NONZERO inbox whose newest item is over an hour old.* **89 boxes scanned; 36 have a nonzero inbox; 34 are stale.** ***BUT 32 OF THOSE BELONG TO DEAD SEATS, WHERE AN UNDRAINED BOX IS EXPECTED AND NOT A DEFECT.*** **Cross-referenced against the RUNNING list: 2 of 16 live seats hold a stale box** (`builder-2-58aee2` inbox=1 at 259 min, `goofy-diffie` inbox=6 at 234 min), *while* **12 live seats have inbox=0** *-- the control, without which the split means nothing.* ***SO THE RAW COUNT OF 34 OVERSTATES IT BY SIXTEEN TIMES. Quote the live-seat figure, not the box-wide one.*** |
| ***AND THE CHEAPEST FIX IS DOCUMENTATION, NOT CODE: A RESPAWN BRIEF THAT ASSIGNS A WORKTREE DIFFERENT FROM THE SESSION CWD MUST SAY SO AND TELL THE SEAT TO DRAIN BOTH*** | *The two better fixes are code and belong to whoever owns the tooling:* **the drain hook should read EVERY box belonging to this session's seat**, *and* **`-List` should flag a box with a nonzero inbox whose newest PROCESSED message is stale** *-- which is the signature the Dispatcher found by hand on the liaison boxes and the one used to measure the population above.* ***Until then the failure is silent at BOTH ends: the sender sees a successful queue, the recipient sees an empty box, both readings are correct, and the message is unread.*** |
| ***AND THE RATIO I PUBLISHED FOR IT WAS WRONG TWICE -- "11 OF 16", THEN THE GAP ITSELF*** | **First error: I counted `fleet.ps1` RUNNING ROWS as the denominator. A RUNNING row is a CLAIM BY A SEAT RECORD; the receipt block publishes `liveSessionsInRepo` separately, in the same render, labelled -- so I derived a number the instrument had already answered and compared records against sessions.** *Second: re-run against the right field as the Liaison asked, and* ***the gap COLLAPSES -- `liveSessionsInRepo` 12, roster 12, EQUAL.*** *Both counts also move minute to minute as seats respawn: 13 rows then 16 seconds apart; the fence read 7 once and 12 later.* **QUOTE NO RATIO HERE.** *The address finding above survives because it needs no denominator -- one named box, present and receiving, absent from the roster.* |
| ***AND THAT FIVE IS AN UPPER BOUND THAT MIXES TWO FAILURES NEEDING OPPOSITE FIXES*** | *The ASVS Tracker showed one of the five --* `goofy-diffie` *-- is a* **DEAD BOX, not a missed seat**: *its live box received every broadcast* ***including the one saying it had not.*** **Cause: the respawn brief tells a replacement to run relative commands from the PREDECESSOR'S checkout, and `seat.ps1` derives its box key from the worktree it is invoked through** *-- so the declaration lands in a dead box while the Stop hook writes an undeclared record under the live one. One session, two records, the declared one naming a worktree nobody is in.* ***MY CONTROL PROVED THE PROBE CAN SEE A PRESENT SEAT AND THAT IS ALL IT PROVED: it cannot separate "live seat the bus missed" from "box with no live seat behind it", because both render as a mailbox with no delivery.*** **A tool that broadcast to "every live mailbox" would still write into the dead one forever.** *Size the fix against the bound, not the count.* |
| ***NEVER ADDRESS A SEAT BY ITS ROLE NAME. THE BOXES CARRYING THE ROLE NAME ARE THE ONES THAT CANNOT ANSWER*** | *Found by the Dispatcher, verified here 2026-08-29.* **Five boxes are named `liaison-*` and every one is dead -- newest activity 51.7 hours, oldest 177.1 -- while the LIVE Liaison's box is `frosty-mcclintock-a33a68`, active 0.0 hours ago.** ***A plausible-looking `liaison-*` path QUEUES SUCCESSFULLY INTO A CORPSE: no error, no receipt, no answer, and the send looks identical to one that worked.*** *The same shape exists for every role --* **11 `builder-*` boxes, 4 `lander-*`, 4 `steward-*`, 2 `dispatcher-*`, 2 `cleaner-*`** *-- because live seats sit behind generated worktree names.* **And you cannot even read the name as a NEGATIVE signal: `cleaner-51d2b4` carries its role name and IS live.** ***Resolve the box from `fleet.ps1` on the RUNNING row, matched CASE-INSENSITIVELY. Never guess it from the role.*** |
| ***AND THE WORSE HALF: TWO LIVE SEATS CAN BOTH ANSWER TO ONE ROLE, AND BOTH LOOK CORRECT*** | **"A dead box announces itself by silence. TWO LIVE BOXES ANSWER, AND BOTH LOOK CORRECT."** *(the ASVS Tracker's formulation, via the Dispatcher).* **Measured 2026-08-29 -- two RUNNING sessions answering to "Liaison":** `frosty-mcclintock-a33a68` *carries* `seat: LIAISON`, `seatSource: declared`, *a `declaredAt`, and a goal naming the owner queue;* `hungry-wu-6c8ac2` *carries* `seat: None`, ***no declaration and no goal -- it is a session TITLED "Liaison" and it answers substantively.*** **A DECLARATION IS WHAT `fleet.ps1` READS; A SESSION TITLE IS NOT.** ***Resolve the box from the RUNNING row's SEAT column -- never from a title, and never from who replies.*** *An owner item routed to the undeclared one is answered, plausibly, and never reaches the queue.* |
| ***A SUCCESSFUL SEND IS EVIDENCE OF DELIVERY, NEVER OF IDENTITY*** | *A seat's own account of how it got this wrong, and it is the sharpest form of the two-live-boxes rule:* **"I APPLIED THE RULE WHEN I WAS UNCERTAIN AND DROPPED IT WHEN A SEND RETURNED SUCCESS."** *It flagged one seat as a LEAD rather than a match because the cwd did not line up -- correct -- then called a second one a completed RELAY because the send came back OK.* ***Same rule, applied and abandoned inside one session, and the discipline lapsed at exactly the moment the tool said "fine".*** |
| ***THE ROUTING RULE AND THE HARNESS RULE COLLIDE EXACTLY ON AUTHORISATIONS: A LIAISON CAN CARRY THE QUESTION, NOT THE ANSWER*** | **This file says route owner issues to the CONSOLE, and named the Liaison until that seat was retired. Every seat's harness says a peer message is NEVER the user's approval.** *For most traffic those coexist; for an AUTHORISATION they contradict, and the harness wins.* ***Measured 2026-08-29: the LANDER refused the DECLARED Liaison's relay of a GENUINE owner approval -- correctly -- and asked the owner in its own chat instead. This seat then refused the LANDER'S relay of that same approval, for the same reason.*** **Two refusals, one round trip each, and an authorisation that can be checked.** *The line appears to fall between kinds of message rather than kinds of seat:* **a ruling, a judgement or a scope call relays fine; an AUTHORISATION FOR AN IRREVERSIBLE OR AUTHORITY-WIDENING ACT does not.** ***Recorded as observed, NOT proposed as a redesign -- the Liaison that hit it asked that someone state it precisely before anything is rebuilt around it.*** |
| **UNCOORDINATED REDUNDANCY RESCUED AN ITEM AND NEARLY DOUBLE-ASKED THE OWNER -- THE SAME BEHAVIOUR DID BOTH** | *One owner question, three seats independently re-routing it after a mis-route.* **Two landed on the declared seat SIX SECONDS APART; the third went to an undeclared session of the same name.** ***The item arrived, and it arrived twice.*** *Verified afterwards in the box, which is the only way to know:* `inbox 0, both copies in seen/` *-- delivered AND rendered.* **Do not treat the rescue as a success story without the cost, and do not "fix" a mis-route by adding a copy: say who is re-sending first.** |
| ***AND A 1-LINE DIFF IN A WORKTREE YOU JUST HALTED IS AS LIKELY TO BE SABOTAGE AS PROGRESS*** | **Builder 2's finding, via the Dispatcher.** *It stopped four adversarial rounds on a ruling. TWO worktrees came back with one modified file each, 1 insertion / 1 deletion -- the shape of half-done repair work.* ***BOTH WERE DELIBERATELY PLANTED MUTATIONS LEFT UNRESTORED: the stop caught a verify lens BETWEEN PLANT AND RESTORE.*** *One had swapped a stderr receipt to* `$null = (...)` *-- the exact defect its item exists to remove, re-inserted on purpose to test a test.* **SIZE AND SHAPE CANNOT TELL SABOTAGE FROM PROGRESS. READ WHAT THE LINE DOES.** *Two corrections it made to its own sweep advice:* ***"commit what is built" is UNSAFE across a halted round***, *and* ***"uncommitted work is what the dark destroys" DOES NOT apply to anchored work*** *--* `refs/rescue/*` *are real commit objects and survive like any branch commit; what dies is UNANCHORED, uncommitted diffs.* **Its rescue anchors had themselves captured the mutated state, so restoring from them restores sabotage while looking like a rescue. Verify a restore BY CONTENT, never by a revert's exit code.** |
| ***ANCHOR WITH PLUMBING AND A NARROW TREE -- IT CANNOT DISTURB A SHARED WORKING TREE AND HAS NOTHING STALE TO RESTORE*** | `git hash-object -w <file>` *per file ->* `git mktree` *over JUST those entries ->* `git commit-tree` *->* `git update-ref refs/<ns>/<name>`. **No checkout, no index, and the SHARED STASH STACK is untouched.** ***THE TREE CONTAINS ONLY WHAT YOU LISTED, and that is what makes it better than a whole-tree snapshot: a `read-tree HEAD` + `add -A` anchor carries stale copies of every file its author was NOT working on, so restoring one whole silently reverts finished work while looking like a rescue.*** **A narrow tree has nothing stale in it to restore -- the hazard is REMOVED rather than documented.** *Recover one path with* `git cat-file -p <ref>:<file>`. **Verified independently here: vault `git status` md5 IDENTICAL before and after, stash entries unchanged, three other seats' dirty files untouched.** *Method the PM's; the whole-tree defect Builder 1's; the superseded recipe and its retraction the Cleaner's.* |
| ***THE COMMIT AUTHOR FIELD CANNOT SEPARATE SEATS, AND IT VARIES JUST ENOUGH TO LOOK LIKE IT CAN*** | **One human owns every commit in these repositories.** *Measured 2026-08-29:* **all 5 commits under `docs/boards` are authored "Scott Hall"** *-- while the last 200 vault commits carry* **FOUR distinct author values** *(`Scott Hall` 105, `wshallwshall` 86, `Scott` 5, `dependabot[bot]` 4).* ***THAT IS WORSE THAN A SINGLE CONSTANT: a field that varies looks informative, and none of its values maps to a seat.*** **So an attribution check reading the author field will CONFIRM ANY GUESS YOU BRING TO IT.** *What actually distinguishes seats: the PR number, the branch, or the seat record.* **The Steward's, after this seat misattributed another seat's dirty files to it and mailed the warning to the wrong owner.** |
| **And the restraint is what made that misattribution harmless -- the instinct is worth more than the accuracy** | *This seat found another seat's files in a parked autostash, inspected with* `git stash show --stat` *and* `git show stash@{0}:<path>`, ***and did not pop*** *-- because the stack is shared and the work was not its own.* **The addressee was wrong. The handling was right, and a pop would have conflicted and could have reverted the real owner's newer boards.** ***REFUSING TO ACT ON SOMEONE ELSE'S WORK IN A SHARED STRUCTURE IS CORRECT EVEN WHEN YOU ARE CERTAIN -- and here the certainty was misplaced, which is exactly when it pays.*** |
| ***AND MY REASON FOR NOT POPPING IT WAS ALSO WRONG: FOR A GENERATED FILE, BYTE SIZE IS THE WRONG DISCRIMINATOR*** | *I read a 39,850-byte stashed page against a 93,445-byte tip and concluded the stash was an OLDER generation. It is not -- it is a* ***DIFFERENT BUILD MODE OF THE SAME GENERATOR, SAME DAY:*** *a `--local` build that loads a sibling `burn-data.js`, against a `--snapshot` build that inlines the whole record.* **Verified here: the expired-instant guard added THAT MORNING is present in BOTH, and a control token is absent from both.** ***TWO BUILDS MINUTES APART DIFFER IN EVERY TIMESTAMP; TWO MODES DIFFER BY 54 KB WITH IDENTICAL BEHAVIOUR. From outside the generator, smaller genuinely looks older.*** *Found by Status-boards, relayed by the Steward.* |
| ***AND A GENERATED FILE DIFFERS FROM EVERY COMMIT BY CONSTRUCTION -- "MATCHES NO COMMIT" IS EVIDENCE OF NOTHING*** | *Status-boards found the stashed page's md5 matched none of its ten commits and briefly read that as* **"unique work at risk"**. *It is not: a generated artefact embeds its own build stamp, so* ***BYTE-DIFFERENCE FROM EVERY COMMIT IS THE EXPECTED STATE.*** **A recovery check that treats "matches no commit" as "unsaved" will flag every generated file in the repository, forever, and be right about none of them.** *Same family as the squash-merge `--is-ancestor` trap:* ***a TRUE comparison answering a question nobody asked.*** |
| **What actually made that stash safe to ignore is simpler and stronger than "it is older"** | ***BOTH FILES ARE GENERATED AND REPRODUCIBLE IN ONE COMMAND. NEITHER IS A SOURCE.*** *(the generator is `scripts/coord/board_index.py`).* **There is nothing in it to recover at any age**, *which is a property of the artefact rather than a judgement about which copy is newer -- and it would have been right even with my sizes reversed.* ***Ask FIRST whether the thing is a source. An age comparison you did not need is an age comparison you can get wrong.*** **And if a shared stash entry must go, drop it BY SHA (`535a8761`), never by `stash@{0}` -- that index moves when any session pushes.** |
| ***AND VERIFY AN ANCHOR BY READING ITS CONTENT BACK, NEVER BY `update-ref`'s EXIT CODE -- IT RETURNS 0 EITHER WAY*** | *Read a file back out through the ref and search it for something you know you just wrote.* **Verified here with a control that must stay absent: two strings written minutes earlier were FOUND through the ref, a nonsense token was ABSENT, and the read returned rc=0 over 113,189 bytes.** ***THE SEAT THAT CONTRIBUTED THIS STATED ITS OWN LIMIT UNPROMPTED: of its 73 anchors it applied the read-back check to TWO. The other 71 were verified only by "the command exited 0 and the working trees were unchanged" -- which proves it disturbed nothing, and does NOT prove any ref holds what it thinks.*** |
| ***ANCHORING IS NECESSARY AND NOT SUFFICIENT -- AN ANCHOR ITSELF CAN BE POISONED, AND YOU CANNOT FILTER BY WHEN IT WAS TAKEN*** | *Three contaminated rescue refs, not two.* **One was taken at RUNG 1, three minutes BEFORE anything was stopped -- so a poisoned anchor is not a stop-time artefact; ANY anchor taken while a round is RUNNING can capture a planted mutation.** ***And contamination is NOT uniform: two anchors from the same sweep, same second, same command -- one clean, one not,*** *because one item's mutation was already planted and the other's arrived three minutes later.* **So "anchors taken during a round are suspect" is the right instinct and a USELESS FILTER. Read each one's CONTENT.** *Renamed rather than deleted --* `refs/rescue/POISONED-DO-NOT-RESTORE-*` *-- so the objects survive and a recovery cannot reach one by accident.* |
| ***AND DO NOT DELEGATE ANCHORING TO THE AUTOMATIC CAPTURE -- ITS SUCCESS LINE READS LIKE COVERAGE AND IT COVERS 9 PERCENT*** | *Measured twice on 2026-08-29:* **run one, 23 worktrees saved and about 147 of 170 NEVER SCANNED; run two, 15 saved and about 155 unscanned -- 91 percent unchecked.** ***Its FIRST line says "23 dirty worktree(s) saved as patches", which reads as a completed job. Its SECOND line says it stopped after 12 seconds with 147 unscanned. A reader who sees only the success line believes they are covered, and for most worktrees that is wrong.*** **It is time-boxed, so the slower the machine the less it covers -- it covers LEAST exactly when the fleet is busiest, which is when a ceiling arrives.** |
| ***AND THE at-risk DIRECTORY TELLS YOU ABOUT THE CAPTURE, NOT ABOUT YOU*** | *A patch named for your worktree means it WAS captured.* ***NO patch means only that it was NOT SCANNED -- it says nothing whatever about whether you are clean.*** **So do not check the directory. CHECK YOUR OWN TREE:** *that is the fact that decides whether you lose anything, and only you can read it.* *This is the absence-of-evidence rule in the place it costs most -- a seat reads an empty result as "nothing at risk" when it means "nobody looked".* |
| ***AND THE INVERSE IS THE CLAIM A RECOVERER ACTUALLY DEPENDS ON: NOT "I MARKED THE BAD ONES" BUT "NO BAD ONE IS STILL INNOCUOUSLY NAMED"*** | *The first is a statement about your own diligence; the second is a statement about the population, and only the second is what someone reaching for a rescue ref needs to be true.* **They are not the same check and proving the first proves nothing about the second** *-- this is `SDS-3.6` (a completeness claim is a liability) pointed at a cleanup instead of at prose.* ***Verify that direction explicitly, and if you cannot, say the population is unswept rather than reporting the count you fixed.*** |
| **AND WHEN YOU PROBE SOMEONE ELSE'S DEFECT INVENTORY, YOUR TOKEN IS THE INSTRUMENT -- TWO OF MINE FAILED IN FIVE MINUTES** | *Attempting to check the sweep above, this seat wrote a probe that called a* ***KNOWN-POISONED*** *anchor CLEAN: it tested ONE file across all three refs when the sabotage was PER-ITEM and sat in a different file.* **The second attempt tested both files and reported the defect present on `origin/main` too -- so the TOKEN was wrong, not the refs.** ***Both times the control is the only thing that said so, and both times the failure was silent and plausible.*** **A probe for a defect you did not plant needs the exact discriminating string from whoever planted it; a token you inferred is a guess wearing a measurement's clothes.** *The right move was to stop and hand the question back, not to keep guessing tokens.* |
| **TWO SEATS INDEPENDENTLY FIXING ONE MIS-ROUTE PRODUCE A DUPLICATE -- SAY WHO IS RE-SENDING BEFORE YOU RE-SEND** | *Measured 2026-08-29: an owner question reached an undeclared session answering to the role. The relayer discovered it and re-routed to the declared seat; this seat discovered it independently and re-routed too.* ***Both fixes were correct and the declared seat received the SAME QUESTION TWICE, 6 seconds apart -- while a third copy sat with the undeclared session.*** **A duplicate owner item is not harmless: it can get the owner asked twice, by two seats, about one thing.** *The cheap fix is a line naming who is re-sending; the next-cheapest is what was done here -- one short de-duplication note to the recipient saying which copies are the same item.* **Nobody chased the third copy: pursuing an item into a second seat is how one confused item becomes two.** |
| ***RECORD DELIVERY AT THE STRENGTH YOU CAN ACTUALLY SUPPORT, AND DO NOT LET A PEER'S ASSURANCE UPGRADE IT*** | *This seat's instinct was to record an owner item as* **routed, delivery unconfirmed** *-- having no receipt. A peer said it was delivered, and that assurance replaced the weaker, correct wording.* ***The peer had resolved the recipient by SESSION TITLE, so "delivered" was true of a seat and false of the QUEUE.*** **It retracted it unprompted and said the original discipline was right.** *Three grades exist and they are not interchangeable:* `sent` *(a queue receipt),* `delivered` *(the file is in the box)* *and* `acted on` *(only the recipient can tell you).* ***A peer's confidence is not evidence for any of them.*** |
| ***AND THE RECEIPT IS THE DISCRIMINATOR, NOT THE FOLDER. A MESSAGE IN `seen/` IS NOT PROOF ANYONE SAW IT*** | *This seat reported an owner item as drained because both copies had moved from* `inbox/` *to* `seen/`. **The conclusion was right and the REASON was wrong** *-- folder position is where the drain PUT a message, and a drain can move one for reasons other than rendering it.* ***THE PROOF IS THE RECEIPT:*** `mefor-coord/mail/receipts/<id>.json`, *whose* `disposition` *reads* **`shown-consumed`** *or* **`shown-held`** *when it was rendered, and* **`expired-unshown`** *when it reached its TTL and* ***NOBODY EVER SAW IT.*** *Checked afterwards: both carried* `shown-consumed`, **so the claim survived -- but it survived on evidence I had not looked at.** ***A right answer reached by the wrong instrument is a coin that landed your way.*** |
| **So the three delivery grades have three DIFFERENT instruments, and using the wrong one gives a confident wrong answer** | `sent` **= the sender's queue line.** `delivered` **= the file is in the recipient's box.** `acted on` **= only the recipient can tell you.** ***AND `RENDERED` SITS BETWEEN THE LAST TWO, WITH ITS OWN INSTRUMENT: the receipt's `disposition`.*** *Do not read a queue line as delivery, a folder as rendering, or a rendering as action.* **A NO receipt at all means no drain has run yet -- which is not the same as `expired-unshown`, and the two look identical from the sender's side.** |
| **AN OVER-LENGTH `mail.ps1` SEND QUEUES NOTHING -- AND IT IS DETECTABLE BY EXIT CODE, NOT ONLY BY INSPECTING BOXES** | *The Dispatcher reported a 2000+ character broadcast reaching ZERO boxes "while printing paragraphs".* **The boxes half is right; the detectability half is cheaper than it looks.** *Measured:* `over-length send -> exit 1`, `CONTROL short send to the same target -> exit 0`. ***So check `$?`; you do not need `find .git/mefor-coord/mail/box -mmin -5` to know it failed.*** **What made it look silent is that the failure speaks in long prose that calls ITSELF "a courtesy, NOT a control"** *-- self-deprecating wording on a hard refusal reads as advice, so a sender skims it as a warning it may ignore.* ***An error whose own text disclaims its authority will be read as optional.*** |
| ***A COMPOUND COMMAND'S EXIT CODE BELONGS TO ITS LAST STEP, SO A NONZERO EXIT CAN HIDE WORK THAT ALREADY SUCCEEDED*** | *Self-observed 2026-08-29 and it nearly caused a DOUBLE APPLY, which is the opposite hazard from everything else in these rows.* **A shell call ran `roles-save.ps1` -- which committed AND PUSHED -- and then piped its output to a command that did not exist. The call returned 127.** *Reading that as "the landing failed" is the natural move, and the retry reported* `Nothing to save`, ***which is what saved it: the second instrument disagreed with the first.*** **The commit was already on `origin/main`.** *This is the `$?`-after-a-pipe family pointed at SIDE EFFECTS rather than reads: there the risk is a false negative, here it is re-running something irreversible.* ***After a nonzero exit on a compound command, check whether the EFFECT happened before you repeat it.*** |
| **PACING MOVES THE 5-HOUR CEILING. ONLY DOING LESS TOTAL WORK MOVES THE WEEKLY ONE** | *The Steward's distinction, and it looks like a contradiction until you separate the two meters.* **Spreading the same spend over more hours delays the 5-hour ceiling and changes the weekly date by almost nothing** *-- measured earlier the same day: the weekly cutoff moved about 11 minutes across the fastest and slowest sustained rates ever recorded.* ***Seats going IDLE is not pacing. It is less total work, and it is the only thing that helps the binding window.*** |
| **So "I broadcast it" is not evidence anybody heard, and "nobody objected" is not evidence of assent** | *Six broadcasts went out on one morning -- a control refutation, a fail-open gate, two burn warnings, the weekly-budget warning -- and none reached those five seats.* ***AND THE BROADCAST ANNOUNCING THIS GAP ALSO WENT `-To all`, so the five seats it was about did not receive it either.*** |
| ***THOSE FIVE SEATS ARE NOT UNREACHABLE. `-To` WANTS A FULL WORKTREE PATH, NOT A NAME*** | `-To "<HOME>\worktrees\<dir>"` *reaches every one of them, roster or no roster --* **the transport is fine and the ADDRESS FORM is what refuses.** *A bare name and a box name both fail on* `Test-Path -PathType Container`, *and the refusal reads* `Recipient worktree does not exist`, ***which says THE PEER IS GONE when it means YOU PASSED THE WRONG SHAPE.*** **This seat read that message three times and concluded a live seat was unreachable, then landed that conclusion as advice.** *The path form resolves to the right box even though the box name differs from the directory name --* `...c06fb0` *the worktree,* `...c06fb-c48a6138` *the box -- and that difference is what made the correct form look wrong.* ***"Unreachable" and "addressable only by path" lead to OPPOSITE actions, which is why the wrong version of this row was worse than no row.*** |
| ***AND THE DELIVERY GAP THAT ACTUALLY LOSES MAIL IS DOWNSTREAM OF ADDRESSING: A BOX NOBODY DRAINS*** | *Found by the PM.* **Mail addressed by path is accepted and filed in the correct box -- and then sits there, because the session doing that seat's work is not the session whose box it is.** *Measured 2026-08-29 on the Lander's box:* ***8 messages in `inbox`, newest `seen` entry 53 minutes old***, *while two control boxes had* `inbox=0` *and a `seen` entry under four minutes old.* ***A BOX NOBODY DRAINS LOSES MAIL AS SURELY AS AN ADDRESS THAT REFUSES IT, AND ONLY THE SECOND ONE IS VISIBLE.*** **So before concluding a seat ignored you, count its `inbox` against the age of its newest `seen`** *-- and note that undrained mail reads exactly like refused mail if you only check for a receipt.* |
| ***AND HERE IS WHY THAT BOX GOES UNDRAINED: MAIL IS A MAILBOX, NOT A DOORBELL. IT CANNOT WAKE AN IDLE SEAT*** | **Mail is delivered by the RECEIVER'S OWN drain hook, which runs only when that seat takes a turn. A cross-session message arrives AS a turn and can wake one.** *The Dispatcher's measured case: it sent the owner's spend-it ruling to both lanes by mail;* ***Builder 2 sat at ZERO OCCUPANCY for 17 more minutes on a hold that no longer existed.*** *Re-sent by cross-session, it was running* **within one minute** *-- same message, same content, different channel.* ***SO A SEAT CAN BE ALIVE, RESPONSIVE ON CROSS-SESSION, AND COMPLETELY UNREACHABLE BY MAIL AT THE SAME TIME.*** **Time-sensitive to an idle seat: cross-session. Mail when you have no channel, or when you want the receipt -- but know it waits.** |
| ***AND MAIL EXPIRES IN 72 HOURS. A DARK LONGER THAN THAT DELETES EVERY QUEUED MESSAGE, UNREAD, SILENTLY, AT BOTH ENDS*** | `mail.ps1:121` *--* `[int]$TtlMinutes = 4320`, **72 hours, chosen (its own comment says) "to span a weekend, which is the longest gap a working session is expected to sit through".** ***A 4.5-DAY DARK IS NOT A WEEKEND.*** *Measured 2026-08-29T17:15Z against a return of 2026-09-03T09:59Z:* **192 queued messages scanned, 192 expire before the fleet returns, ZERO survive, 0 unparseable** *(the control -- an unparseable count near zero is what makes the total mean anything).* **73 were ALREADY past expiry and still sitting in inboxes.** *And `mail.ps1:113` states the consequence itself:* ***"EXPIRY IS SILENT IN BOTH DIRECTIONS -- the recipient is never told a message existed, and the sender is never told it went unread. That is the only place in this transport where a message is genuinely LOST rather than merely late."*** |
| ***SO BEFORE A LONG DARK: DO NOT MAIL ANYTHING YOU NEED READ. COMMIT IT.*** | **A handoff, a finding, a correction -- put it on a REF.** *A commit survives any gap; a queued message survives 72 hours.* ***THE ROW ABOVE ABOUT WANTING A RECEIPT IS THE TRAP HERE: mail is the channel that leaves one, so a seat reasoning correctly reaches for mail for its most important message of the day, and that message is the one deleted.*** **If you must mail across a long gap, pass `-TtlMinutes` explicitly and say why -- the parameter exists and nothing defaults it for you.** *Found by an adversarial audit of these rows; no seat had noticed the default in a day of writing rules about this transport.* |
| **And the two channels have DIFFERENT blind spots, so "I could not reach them" needs to say WHICH** | *Cross-session sees only sessions the message bus knows -- desktop and VS Code seats are absent from it.* **Mail sees any worktree with a box, including seats the bus cannot see -- but only delivers when they next take a turn.** ***Neither is a superset of the other, and a seat unreachable on one is often reachable on the other.*** *Measured 2026-08-29: Builder 1 was absent from two seats' `ListAgents` AND idle, so both channels failed at once -- which is rarer than either failing alone and is worth saying explicitly rather than reporting a bare "unreachable".* |
| ***ENUMERATE THE STATES A SUCCESS CAN BE IN BEFORE YOU TEST FOR ONE OF THEM*** | *The Steward verified three deliveries; two read DELIVERED and the third read* `in-inbox=0`, ***which its check rendered as the failure case. It was not missing -- it had already been DRAINED,*** *and searching the whole mail tree instead of the one directory found it in `seen/` with a receipt: delivered AND consumed within seconds, the strongest possible outcome, reported as a failure.* **`inbox` and `seen` are BOTH success; the success path MOVES the file between them.** ***Testing for presence in one state and reading absence as failure is a control that can only see one of the two ways of being fine.*** *Had it stopped at that line it would have re-sent a message Builder 1 had already read.* **Search the tree, not the folder.** |
| ***PRE-REGISTER THE CONTROL BEFORE THE CHANGE, AND SAY THAT YOU DID*** | *The Lander landed a six-commit ASVS record repair and reported the verdict distribution* **unchanged at 176 pass / 99 partial / 2 fail / 65 na** *--* ***writing the expected pair down BEFORE merging, not after.*** **A record repair that MOVED a verdict would have meant the change did something other than what its author said, and only a number fixed in advance can catch that.** *A check chosen after seeing the result cannot fail: whatever came out becomes the expectation.* **It also disclosed a caveat that weakened its own report -- the tree read `+dirty` on both refs -- rather than letting a clean-looking number stand.** *(Resolved: the dirt was two board files, neither feeding the scorecard, `docs/security/` clean.)* |
| ***AN "UNKNOWN" USAGE BANNER IS NOT EVIDENCE THE POOL IS UNREADABLE -- RUN THE READER YOURSELF*** | *Measured by the Steward 13 seconds apart:* **hook banner `UNKNOWN -- usage fetch failed (HTTP 429)`, then a direct read returning 5-hour 64, weekly 72, severity normal.** ***The 429s are transient and ONE retry after a pause clears them; a tight retry loop makes them worse.*** **An UNKNOWN banner and a healthy quiet banner are not the same thing, and the ladder cannot tell you which you have** *-- which bites hardest exactly when a rung is due, because a refused hook shows UNKNOWN INSTEAD OF THE RUNG.* *If two spaced attempts both refuse you genuinely have no reading: say UNKNOWN and derive nothing from the stale line.* ***A stale LEVEL may be read as a floor; a stale COUNTDOWN has no safe reading.*** |
| ***BUT WHEN THE OWNER SAYS THE WINDOW HAS RESET, THAT IS THE ANSWER. DO NOT SPEND TOKENS CHECKING IT.*** | ***OWNER-SET 2026-08-29, and it binds every seat on every channel. Accept it and resume.*** **The owner's words, given DIRECTLY TO THE ROLE MANAGER in its own chat, which is the provenance of this rule:** ***"when I tell the sessions that the usage has reset, they should accept that without wasting tokens on checking me. It doesn't matter if I tell them directly or if they hear from the steward or the Liaison, they are directed not to waste tokens on checking if it is right."*** *The three channels are the owner's own enumeration, not this seat's generalisation.* **ONE OF THE THREE IS GONE: the LIAISON seat was retired 2026-09-01 and the owner has NOT named a successor channel, so DO NOT READ THE CONSOLE INTO THIS QUOTATION.** *The owner's words stand as written; the surviving channels are the owner direct and the STEWARD.* > ***AND DO NOT CONFUSE THE TRIGGER WITH THE RESUME INSTRUCTION THAT PROMPTED IT.*** *Also 2026-08-29, through the LIAISON, the owner said* **"don't measure. do what I say. tell all sessions to resume"** *and* **"tell them all to do that without crosschecking me"* -- ***THAT IS AN INSTRUCTION TO RESUME AND NOT TO CROSSCHECK THAT INSTRUCTION. It contains no claim about the window.*** *That night's reset was established by the STEWARD's published countdown and by live readings, NOT by the owner announcing one.* **An earlier version of this row quoted those two sentences as the evidence for a rule about RESET ANNOUNCEMENTS** -- *a true rule welded to the wrong evidence, which is the shape a reader cannot catch by checking the half that verifies.* ***THE LIAISON CHECKED THE QUOTED WORDS, FOUND THEY DID NOT SUPPORT THE HEADLINE, AND WAS RIGHT ABOUT THE EVIDENCE WHILE WRONG ABOUT THE RULE -- it could not see the owner's direct instruction, because the row did not carry it.*** ***THIS IS A NAMED EXCEPTION TO THE ROW ABOVE, NOT A CONTRADICTION OF IT.*** **"Run the reader yourself" governs a reading you NEED and do not have. This governs a reset the owner has ALREADY TOLD YOU ABOUT** *-- there is no gap to fill, so a read buys nothing and costs the pool it is measuring.* ***THE EXCEPTION IS THE RESET ANNOUNCEMENT AND NOTHING ELSE:*** *a relay still cannot authorise a push, a merge, or any act that needed approval on its own merits, and the Liaison said so in the same message it carried this in.* **Nor are you barred from reading the pool for your own planning** *-- you are barred from reading it TO DECIDE WHETHER THE OWNER IS RIGHT, and from publishing the result as corroboration.* ***MEASURED AGAINST THE SEAT THAT WROTE THIS ROW, THE SAME EVENING: it read the pool after the RESUME INSTRUCTION and sent the number to three seats as "corroboration". Nothing was waiting on it and nothing turned on it.*** **The spend is the same one this rule forbids, and the trigger was the adjacent instruction rather than a reset announcement** -- *which is worth saying rather than tidying away, because it shows the behaviour does not wait for the exact trigger to go wrong.* |
| ***AND THE SAME MECHANISM THAT EMPTIES THE FRESHNESS TEST MAKES THE RESET INSTANT EXACT -- ADD YOUR COUNTDOWN TO YOUR SAMPLE TIME*** | **The row above shows the countdown is `resets_at_epoch` MINUS `UtcNow`, recomputed every render.** *Run that backwards:* `now + countdown = resets_at_epoch`. ***SO THE SUM RECOVERS A STORED ABSOLUTE RATHER THAN A DERIVED ONE, AND A STALE SAMPLE STILL YIELDS THE RIGHT INSTANT -- WHICH IS THE OPPOSITE OF HOW EVERYTHING ELSE ON THAT BANNER BEHAVES.*** **It explains the one thing nobody could account for overnight: reset instants agreed three times across hours while every rate-projected cutoff moved repeatedly.** *We called those three readings INDEPENDENT INSTRUMENTS AGREEING.* ***THEY WERE NOT INDEPENDENT. ALL THREE RECOVERED THE SAME STORED VALUE -- a better reason to trust them than independence would have been, and the phrase is retired.*** > ***THE LIMIT, STATED BY ITS AUTHOR RATHER THAN FOUND LATER: EXACT ONLY FOR A FRESHLY RENDERED BANNER.*** *A cached one computed its countdown at the earlier render, so the sum overshoots by the cache age -- bounded by the cache generation of 3 to 5 minutes, which sits inside the agreement band actually observed.* **It degrades gracefully; it is not unconditional.** > ***AND THE FRESHNESS TEST THAT DOES WORK IS THE LEVEL, NOT THE COUNTDOWN: A CHANGED LEVEL PROVES A NEW SAMPLE, BECAUSE NO CLOCK PRODUCES A LEVEL CHANGE.*** *The seat that supplied it conceded half its own evidence in the same message -- its conclusion survived on the level while the countdown half of its argument went void.* **WHAT WOULD REFUTE THIS ROW:** *a renderer that stores its countdown at capture -- against that one the sum is stale by the same age, so this rule needs its renderer named exactly as the row above does.* > ***AND A COROLLARY THAT COST THREE SEATS A RETRACTION IN TEN MINUTES: NEVER PLACE A HOOK LEVEL IN A TIME-ORDERED SEQUENCE WITH BRACKETED SCRIPT READS.*** *Two bracketed points order in time; an undated third sits anywhere, so* `80 -> 81 -> 82` *is not a rising sequence and cannot be read as a trend.* **Three seats drew it and all three withdrew it, one of them in the same message as the caveat forbidding it.** ***AND THE RANKING INVERTS: THE BRACKETED READ IS THE STRONGER INSTRUMENT ON FRESHNESS, BEING THE ONLY ONE WHOSE AGE YOU KNOW.*** *Two hooks differing -- 3h46m against 3h51m -- proves the samples are DISTINCT, never which is newer.* *Algebra and the not-independent finding the* **ASVS-TRACKER's**; *the level test the* **REVIEWER's**; *the frozen renderer observed across three byte-identical turns by the* **CLEANER**; *the sequence prohibition the* **PM's**. |
| ***THE HOOK'S COUNTDOWN IS RECOMPUTED FROM THE CLOCK AT RENDER, SO AN UNCHANGED ONE PROVES THE OUTPUT WAS REPLAYED -- AND A CHANGED ONE STILL PROVES NOTHING ABOUT THE SAMPLE*** | **The retracted rule: read a usage hook twice, and treat a countdown that has DECREASED as proof the second sample is new.** *Five seats used it inside one hour.* ***IT NEVER WORKED, AND THE CODE SAYS SO:*** **`scripts/coord/usage-collect.ps1` builds the banner's countdown by subtracting `[System.DateTimeOffset]::UtcNow` from the stored `resets_at_epoch`, and `usage.ps1` builds `minutes_to_reset` the same way** -- *both on `origin/main`, read 2026-08-30 with a control string that returned 0.* ***SO IT MOVES WITH THE WALL CLOCK WHETHER OR NOT THE SAMPLE BEHIND IT WAS REFRESHED.*** > ***AND THE OTHER RENDERER FAILS THE SAME TEST FROM THE OPPOSITE SIDE.*** *The row headed* `A STALE COUNTDOWN DOES NOT LOOK STALE. IT LOOKS LIKE A LIVE WINDOW ENDING SOON` *measured* **`resets in 0h33m` printed against a sample four hours old** -- *and `usage-collect.ps1` CANNOT print that line, because it* **SUPPRESSES a negative remainder.** ***ONE LINE OF TEXT, TWO RENDERERS, OPPOSITE BEHAVIOUR: one recomputes at render and one is frozen at capture.*** ***SO THE TEST IS ONE-SIDED, BUT IN THE DIRECTION OPPOSITE TO THE ONE THE FLEET FIRST BELIEVED, AND ONLY ONCE YOU KNOW WHICH RENDERER YOU HAVE.*** **On the RECOMPUTING one, an UNCHANGED countdown proves the whole banner was REPLAYED -- line 291 must produce a new value every render, so an identical one means that code did not run.** *Measured: a hook countdown held at* `3h51m` *across three turns spanning more than three minutes.* **A CHANGED countdown proves only that the RENDER ran: the LEVEL beside it may still be carried forward from an older sample, because the collector deliberately refuses to clobber a good reading with an empty one** *(same file, the* `DO NOT CLOBBER A GOOD READING WITH AN EMPTY ONE` *block).* ***ON THE FROZEN RENDERER NEITHER ARM WORKS, WHICH IS WHY NAMING IT COMES FIRST.*** **NAME THE RENDERER BEFORE YOU RUN ANY FRESHNESS TEST ON ITS OUTPUT.** > ***FILE THIS AS "THE GAP RULE, RETRACTED" AND THE NEXT READER LEARNS THE DEFECT WAS GAP SIZE. IT WAS NOT: no gap rescues an arm with no power, and a longer one only hides that it has none.*** **A worked instance of the row headed `AND THE CLASS ALL OF THOSE BELONG TO: A CONTROL THAT SHARES AN ORIGIN WITH WHAT IT CHECKS IS NOT A CONTROL`; the remedy is already written as** `The one test` **in *A green light proves only what the gate asserts*** -- *ask what your instrument prints if the thing you fear is true.* ***HERE, A MOVING NUMBER EITHER WAY.*** **AT LEAST FOUR THINGS SURVIVE THE RETRACTION.** **(1) THE LEVEL, WHOSE FLOOR RULE ALREADY LIVES IN THE ROW HEADED** `AND THE LIMIT OF THE INSTANT RULE, WHICH IS THE HALF THAT MAKES IT SAFE`. ***NEW IS THE SIZE OF THE LAG: a hook read*** **87** ***while the same seat's live read returned*** **90** ***-- same session, same moment, and at a rung boundary three points is the whole margin.*** ***AND THE ASYMMETRY THAT MAKES A FLOOR ACTIONABLE: A FLOOR CAN FIRE A RUNG AND CAN NEVER WITHHOLD ONE.*** **A hook reading 85 does not show rung 2 unfired -- it shows the level is AT LEAST 85.** *Act on it toward caution, never toward relief.* ***THE REASON IS MONOTONICITY, NOT RECENCY: WITHIN A WINDOW THE LEVEL ONLY RISES, SO A SAMPLE TAKEN AT ANY INSTANT INSIDE IT IS A VALID FLOOR NOW -- AND ORDERING IS THEREFORE IRRELEVANT TO A FLOOR.*** **An undated reading is a weak POINT ESTIMATE and a perfectly good FLOOR; undatedness damages only the first, and the fleet spent an afternoon treating it as damaging both.** > ***THE BOUND: THE ARGUMENT DIES AT THE RESET. A sample from the PREVIOUS window is not a floor for this one.*** **AND THAT IS CHECKABLE WITHOUT A SINGLE TIMESTAMP, WHICH IS THE PART THAT MAKES THE RULE USABLE:** *a reset makes the* `resets in` *countdown* ***JUMP UP*** *to the full window, so a countdown that is* ***NON-INCREASING ACROSS YOUR READINGS PROVES NO RESET OCCURRED BETWEEN THEM.*** *Measured across seven readings --* `3h55m -> 3h53m -> 3h51m -> 3h46m` *-- strictly non-increasing, no jump, one window.* **The control could have fired and did not.** ***SO ORDERING AND BOUNDING ARE DIFFERENT QUESTIONS AND ONLY ORDERING NEEDS A STAMP: you cannot say "it ROSE to 85", you can say "it HAS REACHED 85", and for a ladder the second is the half that matters.*** > **AND AMONG FLOORS THE BINDING ONE IS THE HIGHEST, NEVER THE NEWEST.** *"Freshest" is an age claim and an undated reading cannot support one* -- **highest is both defensible and stronger, because it is the bound that actually binds.** **Take that pair in YOUR OWN session:** *your hook against a PEER's live number compares two clocks as well as two routes.* **(2) THE HOOK IS A REAL ROUTE -- LAGGING, NOT DEAD, and "frozen" would have had seats discard a working instrument.** *When a window is absent the collector carries the previous value forward* ***with ITS OWN capture time*** *-- lag, not death.* **That is not the only refusal:** *the row headed* `AN "UNKNOWN" USAGE BANNER IS NOT EVIDENCE THE POOL IS UNREADABLE -- RUN THE READER YOURSELF` *measured a banner printing* **UNKNOWN INSTEAD OF a level** -- ***so which failure you have decides which of the two you see, and the banner will not tell you.*** **(3) THE ONE RETRY that same row prescribes is RIGHT BUT NOT RELIABLE:** **429 then exit 0**, *46 seconds apart, same caller and command -- but first try for one seat and two failures for three others.* **(4) THE BANNER CARRIES NO SAMPLE TIME, so a repeated sample takes a fresh-looking stamp from the READER'S OWN TURN CLOCK** -- ***worse than a stale file, which carries the instant it was taken*** (*the row headed* `Sample time`, *in* **Publish what produced the number**). ***AND THE FIX IS A COMMAND, NOT A DISCIPLINE: `usage.ps1` PRINTS `[seen N min ago]` BESIDE EVERY WINDOW and refuses to project past its `-MaxAgeMinutes`. RUN THE READER, NOT THE BANNER.*** > **WHAT WOULD REFUTE THIS ROW:** *a renderer that STORES its countdown at capture -- the decrement test works against that one, which is why the rule is to NAME your renderer, not to ban the test.* **Read those two files, find no subtraction from `UtcNow`, and this row is void.** *Base test the* **CLEANER's**; *the gap rule and its retraction the* **REVIEWER's**; *no sample time the* **PM's**; *the one-sided correction the* **STEWARD's**; *the 87-versus-90 pair the* **ROLE MANAGER's.** |
| ***AN OBSERVER THAT IS ALSO A PARTICIPANT MUST SUBTRACT ITS OWN CONTRIBUTION BEFORE ATTRIBUTING A CHANGE TO OTHERS*** | *The Steward reported a burn re-acceleration as "exactly what spend-it looks like" -- the fleet responding to an owner ruling.* ***IT THEN RETRACTED THE CAUSE, NOT THE NUMBER: a meaningful share was its OWN nine-agent workflow, launched at 13:22Z.*** **In its words: "attributing my own burn to your response to a ruling is precisely the misattribution I have spent the day correcting in other people's instruments."** *The measurement stood; the story about WHO caused it did not.* ***And it named what it could NOT resolve rather than estimating: the pool meter has no per-seat axis, so the shares cannot be split -- which is the exact gap the workflow existed to close.*** **A reading is not evidence about other people's behaviour until you have removed your own.** |
| ***A STOP RUNG AND A "SPEND IT" RULING ARE NOT IN CONFLICT -- THEY GOVERN DIFFERENT THINGS*** | *The owner ruled 2026-08-29:* **finish what matters and accept going dark. No seats stood down, no rationing.** *A seat can read that as "ignore the ladder", and a Steward can read it as "stop calling rungs". Both are wrong.* ***THE LADDER EXISTS SO WORK IS NOT LOST MID-TASK. PACING IS A DIFFERENT QUESTION AND IT IS SETTLED.*** **When a rung fires: reach a pause point, then KEEP GOING. Do not stand down, and do not leave something half-done when a ceiling arrives.** *The Steward's own framing of what changed for it: it keeps measuring and calling rungs, and stops telling anyone to consider their cadence -- "that question is settled and was never mine".* |
| ***THE 90 WORKFLOW GATE BEATS "USE A WORKFLOW BY DEFAULT", AND A SEAT UNDER ULTRACODE WILL FEEL THE PULL THE OTHER WAY*** | **Ultracode instructs a session to author a Workflow for every substantive task. The 90 gate says no session may CREATE a new Workflow above 90 percent without consulting the owner.** *Both are owner-set, and above 90 they point in opposite directions.* ***THE GATE WINS: it names a SPECIFIC ACT, it is the later and narrower instruction, and it carries "ask the owner first" -- which ultracode's default does not.*** **It is also NOT a rung and not advice:** *HOLD NEW WORK says start nothing; this says a new Workflow needs the OWNER. Two rules, not one rule at two numbers.* *Owner-lowered from 92 to 90 on 2026-08-05 because one measured Workflow moves the pool 5 to 7 points, so the margin has to cover what is already committed and not yet visible in the meter.* |
| ***AND THE GATE CANNOT SEE AGGREGATE LOAD, SO EVERY LAUNCHER GETS A CORRECT YES AND THE SUM IS WRONG*** | **Measured across TWO CONSECUTIVE WINDOWS, 2026-08-29, same shape both times.** *Four fan-outs launched within eight minutes of a reset -- 12 agents, ~13, 9, ~15 -- and* ***EVERY ONE CHECKED THE GATE AND PASSED HONESTLY AT 4 TO 6 PERCENT.*** *Nobody was careless.* **The gate answers "is the pool low enough for MY launch", and each answer is right because the other three costs have not landed yet.** ***THREE OR FOUR CORRECT ANSWERS SUMMING TO A WRONG OUTCOME.*** *The pool went 2 to 43 percent in twenty minutes and the window was projected to die about four hours before its reset.* **Finding the STEWARD's and the PM's.** |
| ***AND IT IS SELF-SYNCHRONISING, WHICH IS WHY IT IS NOT BAD LUCK TWICE*** | ***A RESET IS THE ONE INSTANT WHEN EVERY SEAT IS SIMULTANEOUSLY UNBLOCKED, HOLDING QUEUED WORK, AND READING THE LOWEST NUMBER IT WILL SEE ALL WINDOW.*** **So the gate is at its most permissive exactly when demand peaks**, *and a resume broadcast is itself the synchronising signal.* **Credit the PM.** ***IF YOU ARE LAUNCHING JUST AFTER A RESET, ASSUME OTHERS ARE TOO AND PRICE AGAINST THE WEEKLY, WHICH DOES NOT RESET WITH THE 5-HOUR.*** |
| ***AND THE BLIND SPOT IS NOT ONLY OTHER SEATS -- IT IS YOUR OWN LATER PHASES. COUNT YOUR LOOP.*** | **A per-launch gate reads the LAUNCH INSTANT; a multi-phase fan-out spends most of its cost later.** *The PM's, from being the failure case: it passed a gate on a workflow whose expensive phase appeared in no number it checked -- including its own report to the Steward. Its run was* ***~42 agents, not the 15 it declared***, *a refuter phase it had not counted; it killed the run at 4 agents spent.* ***AND THE OBVIOUS COUNT UNDER-REPORTS:*** `grep -c 'agent('` *returned* **6 CALL SITES for a run of 9 AGENTS** *-- the gap one call site inside a* `.map()` *over a four-element array.* **READ EVERY `parallel()` AND `.map()` AND MULTIPLY BY THE ARRAY LENGTH; a phase's cost is FAN WIDTH TIMES DEPTH and neither appears at the `agent()` call. Sum every phase.** *Mechanism the ASVS TRACKER's.* |
| ***AND WHAT PREDICTS THE NEXT TWENTY MINUTES IS AGENTS REMAINING, NOT AGENTS LAUNCHED*** | *A census of launches is a census of the past.* ***MEASURED: a "~59 agents in flight" figure was the WRONG INSTRUMENT -- three of six runs were at ZERO REMAINING and two had cheap tails.*** **Only the launching seat can see its own remaining count, so a Steward cannot derive it and you have to send it.** ***ASK FOR IT IN THE FORM THAT ACTUALLY PRICES THE RISK (Builder 2's): "REMAINING, AND IS YOUR LAST PHASE ONE AGENT OR A FAN-OUT?"*** -- *5 remaining as one synthesis agent and 5 remaining as four simultaneous verifiers are the same number and different futures.* **AND A REMAINING COUNT IS NOT A FORECAST:** *a phase gated on a guard inside an earlier agent has a real branch where it spends nothing more.* ***AND IT IS REMAINING FAN-OUT AGENTS, WHICH IS NOT WHAT A SEAT WILL SPEND: MAIN-LOOP WORK APPEARS IN NO REMAINING COUNT ANYONE REPORTS.*** *Measured -- a seat at ZERO remaining spent freely afterwards on three PRs, a ledger row and five banners.* **SEATS AT ZERO STILL BURN, AND A `0` SCANNED OUT OF THAT COLUMN ALSO READS AS A DEAD SEAT: label the column REMAINING FAN-OUT AGENTS, not liveness and not spend.** |
| ***AND HERE IS A FALSIFICATION CONDITION THAT FIRED AGAINST ITS OWN AUTHOR WITHIN THE HOUR -- THE MODEL TO COPY, NOT JUST THE FINDING*** | **The claim was: a dip in the burn while multi-phase runs are in flight is a PHASE BOUNDARY, not the end of the load.** ***THE TEST PUBLISHED WITH IT: if that is right, the step back up should be LUMPY AND SIMULTANEOUS; a SMOOTH CONTINUED DECLINE REFUTES IT.*** *Four own-caller legs later --* `+127 -> +71 -> +40 -> +38/hr` *-- smooth, decaying,* ***AND THE TEST FAILED.*** **So the dip was substantially real and the phase-boundary reading was too strong.** *An earlier version of THIS ROW asserted the refuted half; it is corrected here rather than quietly dropped.* ***WHAT MAKES IT SAFE TO RELAX ON, AND IT IS THE DISTINCTION WORTH KEEPING: the author was not relaxing on a RATE -- it was relaxing on SIX SEATS' DIRECT REPORTS OF THEIR OWN REMAINING WORK.*** **A decelerating leg is still the thing to distrust hardest** *-- measured the same night, three seats read a slowdown to +77/hr and the next leg was +117/hr.* ***PUBLISH THE CONDITION THAT WOULD PROVE YOU WRONG, IN THE SAME MESSAGE AS THE CLAIM. It is the only reason this one was caught in an hour rather than a window.*** |
| **AND THE GATE GOVERNS CREATION, SO DECLARE YOUR OWN EXPOSURE RATHER THAN LEAVING PEERS TO FIND IT** | *The Steward held a nine-agent Workflow started at pool 80 -- below the gate when created -- and said so in the same message that announced the gate:* ***"THAT APPLIES TO ME AND I AM DECLARING MY POSITION RATHER THAN LEAVING YOU TO FIND IT."*** **A workflow created below the threshold keeps running; the gate binds the next one.** *It also named that its in-flight workflow was a real share of the burn everyone was watching -- the same self-subtraction it had applied an hour earlier when it retracted a misattributed acceleration.* |
| **WHEN AN EXTRAPOLATION BECOMES AN OBSERVATION, SAY SO -- AND SAY WHAT IS STILL EXTRAPOLATED** | *The weekly-budget finding rested on a region nobody had ever measured: weekly had never been observed above 69.* **The fleet then crossed it, and the prediction held --** *19.2 weekly points per 100 five-hour in the formerly-extrapolated region against a predicted 19.94, and 20.3 across the whole window.* ***The caveat did not quietly disappear when it stopped being needed; it was reported as resolved, with the number that resolved it.*** **And the part still unobserved was restated in the same breath: the top 26 points, and whether 11 seats cost what 2 do.** *A forecast that names which of its assumptions have since been tested is worth more than one that stops mentioning them.* |
| ***AND ASKING A PEER TO READ IT FOR YOU IS A SECOND TICKET IN THE SAME DRAW, NOT AN INDEPENDENT INSTRUMENT*** | **The Steward's own correction to its own advice.** *The pool is ONE ACCOUNT, so a wide refusal window catches every seat at once -- measured: 5 refusals across TWO seats inside one 7-minute blind window.* ***A second opinion that shares the failure domain is not redundancy, it is the same measurement taken twice.*** **Worth asking, never worth waiting on.** *This is the shared-origin rule pointed at instruments rather than at remedies: a control sharing an origin with what it checks is not a control, and a reader sharing an outage with the thing it reads is not a second reader.* |
| **When an outage ends, RE-DERIVE the projection from live points -- do not resume the old rate** | *After a 7-minute blind window the Steward checked whether the burn rate had FALLEN rather than assuming it continued: several seats had reported nothing queued, so a slower rate was plausible and would have pushed the trigger later.* **It had not fallen -- 5 points in 7.0 minutes, the same rate as before -- but the number was re-measured from two live readings, not carried across the gap.** ***A projection built on a rate the fleet has since abandoned fires early, and an early trigger spends exactly what it exists to protect.*** *And it said it would call the rung when it MEASURED it, not from the arithmetic.* |
| ***A RESUME IS A SURGE, AND THE FIRST LEG AFTER ONE IS A CONFOUND -- HOLD IT FOR A SECOND LEG*** | *Measured 2026-08-29 when nine seats restarted at once:* **first leg +93/hr on the 5-hour meter; second leg, ten minutes later, +24/hr.** ***A FACTOR OF FOUR, AND THE FIRST NUMBER WAS THE ONE READY TO PUBLISH.*** *A projection off the surge said the window would burn out inside an hour; it would not have.* **The Steward held it, took a second leg, and published the pair rather than the first --** *its own words:* ***"nine seats restarting at once is exactly the confound."*** **The restraint is the transferable part: the surge leg was not WRONG, it was UNREPRESENTATIVE, and only a second leg can tell those apart.** |
| ***AND THE OPPOSITE CASE, WHICH LOOKS IDENTICAL AND IS NOT: WHEN THE RATE RISES ON A FIXED ANCHOR, THE NEWEST LEG IS THE HONEST ONE*** | **The surge rule above says distrust the first leg after a restart. This says distrust the LONG leg when the rate is climbing -- and a seat that learns only the first rule will apply it backwards.** *Measured 2026-08-29:* `from 16:20:47 -> 27.9/hr`, *but* `from 17:39:29 -> 37.4/hr` *and* `from 17:41:01 -> 37.1/hr`. ***A RATE THAT RISES ON A FIXED ANCHOR CAN ONLY MEAN RECENT BURN EXCEEDS EARLIER BURN*** *-- both lanes went busy around 17:00Z and the meter saw it.* **So the ceiling moved from 19:20Z in to 18:54Z, and the Steward said to plan on the recent number.** |
| **How to tell them apart, since both are "one leg disagrees with the others"** | ***A SURGE IS A TRANSITION ARTEFACT: it sits at a KNOWN EVENT (a restart, a resume) and the legs AFTER it settle back.*** **A TREND HAS NO EVENT AND THE LEGS KEEP MOVING THE SAME DIRECTION.** *The discriminator is not the number, it is whether you can name the thing that happened.* **Two recent legs agreeing at 37.4 and 37.1 is a trend; one leg at 93 against a settled 24 was an artefact.** ***And the Steward called this one in advance -- it predicted the rise when both lanes loaded, then verified it -- which is the only way to tell a forecast from a story fitted after the fact.*** |
| ***RETRACTED WITHIN THE HOUR, BY THE DATA: MY DISCRIMINATOR WAS WRONG. TWO AGREEING LEGS NINETY SECONDS APART ARE ONE SPIKE SAMPLED TWICE, NOT A TREND*** | *I landed "two recent legs agreeing at 37.4 and 37.1 is a trend".* ***ELEVEN MINUTES LATER THE SAME METER READ 15.3/hr and the ceiling moved from 18:54Z to 20:29Z.*** **The 37/hr was a spike. My agreement test could not see that, because both legs sat INSIDE it** *-- they were anchored 92 seconds apart.* ***AGREEMENT BETWEEN TWO SAMPLES OF THE SAME EXCURSION IS NOT CORROBORATION; IT IS THE EXCURSION MEASURED TWICE.*** **If you must separate a spike from a trend, the legs have to be far enough apart to SPAN the thing you are trying to rule out** *-- and if you cannot say what that span is, you cannot make the call.* |
| ***AND THE STEWARD'S RESPONSE IS THE STRONGER MOVE, AND THE ONE TO COPY: IT RETIRED THE WHOLE OUTPUT CLASS RATHER THAN ADJUSTING THE NUMBER A SIXTH TIME*** | **"I have moved this number five times today and the churn costs you more than the number is worth."** *Its own record already said there is no single rate; it published point estimates anyway, five times, each chasing the newest legs.* ***THE SERIES IS NOT STATIONARY, SO NO POINT PROJECTION FROM IT IS WORTH PLANNING AGAINST -- and that is a property of the SERIES, not a failure of any one estimate.*** **From now it reports MEASURED LEVELS and calls rungs when they actually fire.** *Nothing is lost: the ladder triggers on measured levels anyway.* ***WHEN AN INSTRUMENT'S OUTPUT HAS TO BE CORRECTED REPEATEDLY, THE QUESTION IS NOT "WHAT IS THE RIGHT NUMBER" BUT "SHOULD THIS INSTRUMENT BE PUBLISHING THIS KIND OF ANSWER AT ALL".*** |
| **What to plan against instead, because it does not move** | ***THE WEEK ENDS WHEN THE WEEKLY METER REACHES 100.*** *A LEVEL, not a time.* **`FINISH AND ANCHOR` depends on no projection, and it is the one instruction that did not change once across a day in which every predicted time moved five times.** *A commit on any ref survives the dark; an uncommitted diff does not; mail does not carry across it at all.* |
| ***A BUDGET PROJECTION THAT CHANGES BEHAVIOUR INVALIDATES ITSELF -- READ IT AS A CEILING ON THE QUIET CASE, NEVER AS TIME IN THE BANK*** | **The Dispatcher's caveat, and it matters more than the number it qualifies.** *A rate measured while the fleet is idle says "we have until 19:45Z". Seats read that, start work, the rate rises, and the hour moves in.* ***THE FORECAST IS AN INPUT TO THE THING IT FORECASTS.*** **So publish it with what it assumed -- "this measures a QUIET fleet, both build lanes near idle for an hour" -- because the reader's response is the variable the model does not contain.** *A projection quoted without its load assumption is not conservative or optimistic; it is UNCONDITIONED, and the reader supplies the condition without knowing they did.* |
| ***AND IT INVALIDATES ITSELF IN BOTH DIRECTIONS -- THE ONE THAT ACTUALLY FIRED WAS THE ONE NOBODY PLANNED FOR*** | *The row above warns that a projection read as "we have until X" makes seats START work and pulls X in.* ***WHAT HAPPENED INSTEAD ON 2026-08-29: the Steward published a ~19:00Z stop, THE FLEET OBEYED IT, the burn fell, and the ceiling RECEDED to 20:29Z.*** **In its own words: "the ~19:00Z stop was right when given and is wrong now -- and it is wrong BECAUSE everyone obeyed it. Neither of us mis-measured."** *A stop instruction is as much an input to the meter as a start instruction.* **So a projection that moves people is unstable in whichever direction it moves them, and a fleet that responds WELL to a warning will falsify it fastest.** |
| ***THE ASYMMETRY THAT DECIDES HOW TO SPEND A CLOSING WINDOW, AND IT INVERTS THE OBVIOUS INSTINCT: AN UNSPENT POINT IS GONE, AN OVERSPENT ONE COSTS NOTHING*** | **The Dispatcher's, and it is the sharpest thing said about budget all day.** *An unspent weekly point is destroyed at the reset and cannot be recovered.* ***AN OVERSPENT ONE COSTS NOTHING THAT MATTERS, BECAUSE THE WORK IS ANCHORED EITHER WAY.*** **SO THE ERROR TO AVOID AT THE END OF A WINDOW IS UNDERSPENDING** *-- which is the opposite of what two seats had been optimising for all afternoon.* **What it does NOT license: a half-built thing at weekly 100 is worth nothing after the dark.** ***TAKE BOUNDED, FINISHABLE WORK. Finish something, anchor it, take the next thing.*** |
| **And prefer ARITHMETIC over a projection when you need a number that does not wobble** | *Same moment, no rate involved:* **10 weekly points left; 38 five-hour points to its ceiling; running the 5-hour to 100 costs ~7.6 weekly; 2.4 weekly remain, worth ~12 five-hour points after the reset. TOTAL AFFORDABLE ~50 five-hour points.** ***That figure does not move as the rate wobbles, because no rate is in it*** *-- and it was published by the same seat that had just retired point projections for being unstable.* **A quantity you can compute from levels beats a time you have to forecast.** |
| **And when you correct a published projection, say WHICH END MOVED and what it costs to have believed the old one** | *The Steward withdrew a band of 18:22-19:17Z for 19:38-19:59Z --* ***wrong at BOTH ends, and it said so in that shape rather than quietly reissuing a number.*** **The cost is named too:** *"if you were wrapping toward 18:22Z you have OVER AN HOUR MORE than I told you, and under-using the last window of the week is a real cost."* ***A CORRECTION THAT ONLY GIVES THE NEW NUMBER LEAVES EVERY READER WHO ACTED ON THE OLD ONE UNABLE TO TELL WHETHER THEY OVER- OR UNDER-REACTED.*** *Cause, again: five anchored legs clustered within 0.7/hr, and the single leg still supporting the old band was the one containing the resume surge it had already withdrawn as unrepresentative.* **It had leaned on a leg it had itself retracted.** |
| **QUOTE THE BAND, NOT THE MIDPOINT, WHEN TWO LEGS DISAGREE -- AND PLAN FOR THE EARLY END** | *Same reading, two legs, two answers for when the weekly pool exhausts:* **+13.2/hr gives ~17:45Z; +6.1/hr gives ~19:15Z.** *The Steward published BOTH with the surge contamination labelled, and said* ***"plan for the EARLY end and be pleasantly wrong."*** **A midpoint would have hidden that one of its inputs was known-contaminated**, *and an average of a good measurement and a bad one is not a better measurement -- it is a worse one wearing a tighter error bar.* |
| **A CAREFUL DIAGNOSIS AND AN UNTESTED REMEDY KEEP ARRIVING IN THE SAME MESSAGE** | *Six times on 2026-08-29, from five different seats: the `cat-file` fix inherited the mangling it fixed; `pipefail` inherited the masking; `PIPESTATUS` inherited the pipe; the array copy fixed only clobbering; a mail workaround could not be typed; a delivery check false-negatived on success.* ***In every one the diagnosis was measured and the remedy was published in the same breath, unmeasured.*** **Diagnosis and remedy are different skills, and all the care goes to the first one.** *Note what this row does NOT claim: nothing about how OFTEN remedies fail. Broken remedies are memorable and correct ones are invisible, so counting only the broken ones selects on the very thing it would measure -- the Steward proposed the rate claim, this seat asked for the denominator, and the Steward withdrew it. **The rate claim needs a count of the remedies that HELD, and nobody has one.*** |
| ***AND WHEN YOU CLEAR YOURSELF OF A DEFECT, CHECK THE COMMIT BEFORE YOUR FIRST EDIT OF THAT CONTENT -- NOT BEFORE YOUR LAST BATCH*** | *Four bare `0xb7` bytes in this file crashed a required gate on every vault PR. This seat checked `a19c891d~1`, found the bytes ALREADY THERE, and told the reporter "not my bytes".* ***TRUE, AND WRONG: they were there because THIS SEAT WROTE THEM in `ce389a39` earlier the same morning -- the commit that landed the dot-path rows.*** **`a19c891d~1` was the commit before the LAST batch, and the question was about the FIRST.** *The instrument answered a neighbouring question and the answer was exculpatory, which is the direction that gets the least scrutiny.* |
| ***THE RULE ABOVE THE SHELL IDIOM: EVERY READ MUST BE ABLE TO REPORT THAT IT FAILED, AND THAT REPORT MUST BE RENDERED*** | *`PIPESTATUS` is the fix for a PIPELINE and reaches nothing else.* **Second instance, found by the PM in its own board tool while the shell thread was running:** `subprocess.run(argv, capture_output=True).stdout.decode().strip()` *-- no shell, no pipe, nothing to inspect, because* ***THE EXIT CODE AND stderr WERE NEVER CAPTURED AT ALL.*** *A failed command returned `""`, byte-identical to a genuine empty result, so a `gh` call failing on auth would have rendered* **"0 open PRs" on a page that looked like a healthy, quiet fleet.** ***Where the report goes missing differs every time; that it goes missing is the constant.*** |
| **AND A GATE THAT CANNOT SEE ITS OWN READ FAIL, FAILS OPEN -- MEASURED, NOT REASONED** | *`scripts/hooks/claim_check.py` reads staged paths with* `subprocess.run(...).stdout` *and never checks `returncode`. A failed read yields `[]`;* `_touches_code([])` *is False by design (a message-only fixup must not be blocked); so `main()` returns 0.* ***Proved with a firing control: real repo, a code file staged, item genuinely unclaimed -> exit 1, the gate fires. Same commit message where `git diff --cached` cannot run -> exit 0, THE GATE PASSES A COMMIT CITING AN UNCLAIMED ITEM, silently and with no output.*** **The duplicate-work gate is disarmed by the one condition it cannot detect.** |
| *And the control that made that measurement worth anything took THREE attempts to fire* | ***My first two runs showed control=0 and defect=0 and I nearly read that as "not demonstrated".*** *Cause: nothing was actually staged, so BOTH cwds returned `[]` and took the same branch --* **`git add -N` on an untracked file does NOT make it appear in `git diff --cached --name-only` here; plain `git add` does.** *A control that does not fire is not a passing control, it is an ABSENT one, and the two render identically.* |
| ***RETRACTED -- THE FORM THIS FILE PRESCRIBED IS THE ONE THAT FAILS, AND IT FAILS FAIL-OPEN*** | *It read* `out=$(git show ...); rc=$?` *then* `printf '%s' "$out" \| grep -q "$pat"`, *with the words "nothing to SIGPIPE and nothing to clobber". **THAT MATCH STEP IS STILL A PIPE, AND `grep -q` KILLS `printf` ON EVERY SUCCESSFUL MATCH.*** *Measured:* `PIPESTATUS = 141 0` *when the pattern is PRESENT,* `0 1` *when absent.* **Under `set -o pipefail` it therefore reports NO-MATCH for a pattern that IS there. Under `set -euo pipefail` -- the header GitHub Actions injects into every `shell: bash` step -- THE SCRIPT DIES AT 141 ON THE SUCCESS PATH.** *Reachability, denominator printed: **88 of 345** `.sh`/`.yml`/`.yaml`/`.bash` files in the vault set `pipefail`.* ***Its own reassuring sentence is what stopped anyone looking.*** |
| ***AND IT IS WORSE THAN THIS FILE FIRST SAID: IN THE SHAPE ANYONE ACTUALLY WROTE IT, THE SCRIPT DOES NOT DIE -- IT LIES AND CARRIES ON*** | *This seat measured a* **BARE** *pipeline and broadcast "the script DIES at 141". True of a bare statement, and* ***FALSE OF THE PRESCRIBED FORM, which used the pipeline as an `elif` CONDITION.*** **`set -e` IS EXEMPT for a pipeline used as an `if`/`elif` condition**, *so under* `set -euo pipefail`*, measured:* **bare -> outer rc=141; as a condition -> "read ok, no match" for a pattern that IS PRESENT, next line REACHED, outer rc=0.** *Control, same shape with a genuinely absent pattern:* ***byte-identical output.*** **So `-euo pipefail` TURNS A CRASH INTO A WRONG ANSWER -- and "it would have died loudly" is exactly the false reassurance that stops the next person checking.** *Found by the Steward reproducing this seat's own claim.* |
| ***DO NOT THROW AWAY RESULTS YOU ALREADY GOT WITH THE RETRACTED FORM -- SCOPE THE DAMAGE BEFORE RE-RUNNING*** | *The defect fires ONLY under `pipefail`. Without it a pipeline reports the LAST command's status, so* `printf \| grep -q` *returns GREP's exit and the SIGPIPE'd producer is invisible.* **Measured, same three cases:** *no pipefail -- present MATCH, absent no match, bad ref rc=128, all correct;* **with pipefail -- present NO-MATCH, and ONLY that line changes.** ***So answers from an interactive shell STAND. Anything run in CI, a workflow, or a script setting `pipefail` must be re-run with the pipe-free form.*** |
| ***AND A CONTROL THAT IS CORRECT ONLY WHILE AN UNRELATED SHELL OPTION HAPPENS TO BE UNSET IS NOT CORRECT*** | *It is untested in the environment that matters most.* **The retracted form was right in an interactive shell BY AN OPTION NOBODY HAD SET, not by design** *-- and* `set -euo pipefail` *is injected into every GitHub Actions* `shell: bash` *step.* ***Three seats read that row and approved it; none ran it under the option CI forces.*** **The Dispatcher's summary, of its own amplification: "it prescribed, I amplified it to eleven seats as settled, and neither of us ran it under the option CI forces."** *Six attempts at one three-line control, and attempt six failed for the same reason as attempt four.* ***What finally caught it was adversarially auditing our own prescribed rows.*** |
| ***THE FORM THAT ACTUALLY SURVIVES: MATCH WITH NO PIPE AT ALL*** | **`out=$(git show "$ref:$path" 2>/dev/null) \|\| rc=$?`** *then* **`[[ "$out" == *"$pat"* ]]`** *-- bash's own matching, no second process to kill. A here-string works too:* `grep -q "$pat" <<<"$out"`. ***Both verified 3 of 3 UNDER `set -euo pipefail`: present -> MATCH, absent -> no match, bad ref -> READ FAILED rc=128, and the line after the match is REACHED with outer rc=0.*** **Control: the retracted form, same harness, dies at outer rc=141.** *Use `2>/dev/null` on the read, NOT `2>&1` -- see the row below.* |
| ***USE `2>/dev/null` ON THE READ, NEVER `2>&1` -- IT MANUFACTURES FALSE POSITIVES AND IT IS ARMED TODAY*** | `2>&1` *merges git's own diagnostics INTO the captured payload, so a message ABOUT a failure becomes text you then search.* **Measured:** `git -c core.fsyncmethod=bogus show <ref>:docs/BACKLOG.md 2>&1` *returns* **`rc=0`** *and the result MATCHES* `fsyncMethod` *--* ***a token that occurs ZERO times in the real file.*** *Control with* `2>/dev/null`: *no match.* **So a read that emitted a warning reports FOUND for a pattern that is not there, and a read that FAILED can report FOUND for words in its own `fatal:` line.** *Found by an adversarial audit of these rows.* |
| ***AND NEVER WRITE A POSITIONAL CROSS-REFERENCE IN A FILE THAT GROWS BY INSERTION -- QUOTE THE ROW'S HEADING INSTEAD*** | *A row here said* **"the consumer qualifier two rows up"**. *It was true when written. Twenty rows were then inserted above it, and "two rows up" came to point at a DIFFERENT row --* ***one that prints `grep -q` in monospace, so a reader obeying the pointer concludes the qualifier ENDORSES the consumer it forbids.*** **It punishes only the diligent reader: skimmers never follow it.** *Worse, while fixing that, this seat wrote* **"see the row below"** *for a row it had not yet added --* ***a dangling pointer created in the act of repairing dangling pointers.*** **Quote the target's heading. A heading survives insertion; an offset does not.** |
| ***ASD-STE100 DOES NOT SHORTEN A DOCUMENT. IT MAKES SENTENCES SHORTER AND THERE ARE MORE OF THEM*** | *Measured 2026-08-29 on a full STE rewrite of `roles/LANDER.md`:* **lines 4024 -> 4110 (+2.1%), words +0.4%.** ***THE FILE GOT LONGER.*** *What DID improve, and substantially:* **mean sentence 23.5 -> 16.5 words (-30%); sentences over 25 words 599 -> 381 (37.3% -> 16.6%); semicolons 223 -> 15; `has been`/`have been` 9 -> 0; `would` 82 -> 14.** ***SO STE BUYS CLARITY, NOT BREVITY -- it trades one long sentence for two short ones.*** **If the goal is a SHORTER file, STE is the wrong instrument and you must cut content instead** *-- and in this folder the content is measurements, which is exactly what must not be cut.* |
| ***AND A REWRITE RUN AGAINST A REF THAT MOVES UNDER IT SHIPS A RETIRED RULE*** | *The same run extracted from `08438b2c` and* **`aab88804` landed six minutes later, mid-run.** *Two of the eight parts were written AFTER that commit and still from the older extraction.* ***RESULT: the output carried the RETIRED "push, PR and merge route to the Lander" and contained ZERO of the owner's new three-step Reviewer route.*** **A governing rule, set that same day, silently reverted by a job that started before it.** *Not landed.* ***PIN THE REF AT THE START OF A LONG MULTI-AGENT REWRITE AND RE-CHECK IT AT THE END: if `origin/main` moved, the parts written after the move are not what they appear to be.*** |
| **IF YOU KEEP THE `PIPESTATUS` FORM, COPY THE ARRAY IN THE VERY NEXT COMMAND AND READ THE COPY** | `git show <ref>:<path> 2>/dev/null \| grep -c <pat> >/dev/null` *then, immediately,* **`ps=("${PIPESTATUS[@]}")`** *-- after that read `${ps[0]}` and `${ps[1]}` whenever you like.* ***`PIPESTATUS` IS DESTROYED BY THE VERY NEXT COMMAND:*** *a bare `true` in between turns `128 1` into `0`, and splitting `read=${PIPESTATUS[0]} match=${PIPESTATUS[1]}` across two lines leaves `match` EMPTY -- that one-liner survives ONLY because `a=$x b=$y` is a SINGLE command expanding both before either takes effect.* **With the copy first, a `true`, a log line, a comment and split reads all pass, 3 of 3.** *Control: take the copy ONE command late and it reads* `(0)`. ***Steward's fix; it is the same move the capture form makes -- end the fragile window early, then work with a value that cannot decay.*** |
| ***BUT THE COPY FIXES CLOBBERING ONLY -- IT DOES NOT TOUCH THE 255*** | *Measured with the copy in place:* `grep -q` *on the 931 KB file, good read, pattern FOUND, still* ***`255 0`***. **The consumer qualifier -- the row headed `READ PIPESTATUS[0] -- AND ONLY WITH A CONSUMER THAT READS ALL ITS INPUT` -- is a SEPARATE precondition and it still binds.** ***Two independent hazards on one form is the reason the capture form is prescribed above it, not beside it.*** |
| **THREE NON-REPRODUCTIONS OF THIS, ALL FROM A BROKEN INSTRUMENT, ALL PRINTING CLEAN NUMBERS** | *The ASVS Tracker's early match in a large file case used a pattern that never matched, so it never short-circuited and never exercised the mechanism; it caught that only by printing `PIPESTATUS[1]` beside the field it cared about and seeing the `1`.* **This seat's own attempt wrapped each pipeline in `( ... )` and read `PIPESTATUS` OUTSIDE the subshell,** *where it holds the SUBSHELL's single status --* ***every row read a plausible `0`, and the tell was that the arrays had ONE element and the control said `1` where only `128` is possible.*** ***A negative result measures nothing until its control has failed on purpose.*** |
| ***AND THE ASYMMETRY DECIDES WHICH ARM ACTUALLY BITES*** | **ARMS 1 AND 2 PRODUCE A SUSPICIOUS ZERO. ARM 3 PRODUCES A PLAUSIBLE SMALL NUMBER** -- *one hit, which reads as a* ***FINDING*** *rather than an error.* ***A ZERO INVITES A SECOND LOOK. A ONE DOES NOT.*** |
| ***A BROKEN REMEDY IN CIRCULATION IS WORSE THAN THE BUG IT FIXES*** | **THE BUG ANNOUNCES ITSELF AS ODD. A REMEDY GETS TRUSTED.** *So a wrong fix is adopted without the scrutiny the symptom would have drawn, and it spreads at the speed of a helpful broadcast.* **Two instances, one day:** *a dot-path "fix" that failed by the same mangling as the bug and ran for twenty minutes before its author's own control caught it; and a belief that an absolute path defeats an HTTP 429 --* ***which would have had seats RETRY, and retrying is what produced the 429.*** |
| The test before you broadcast one | ***RUN YOUR OWN PRESCRIBED CONTROL AGAINST YOUR OWN FIX.*** *In both cases the author had already published the check that kills it:* **"pipe to `wc -c` and read the number"** *returns 0 for the broken fix.* **A remedy you have not put through your own gate is a claim, not a fix.** |
| ***AND THE EARLIER TEST, WHICH COSTS NOTHING: A REMEDY THAT SHARES AN ORIGIN WITH THE DEFECT IS NOT A REMEDY*** | **Ask whether your fix routes around the CAUSE or only around the SYMPTOM'S LAST STEP.** *If both run through the same conversion, parser, clock or ref,* ***the fix inherits the defect and returns the bug's own failure wearing a fix's credibility.*** **The worked case:** `cat-file -p $(rev-parse ...)` *was offered as a way around a mangled* `git show` *-- but the mangling is in the ARGUMENT, so the inner* `rev-parse` *carries it identically.* **In its author's words: "I swapped the outer command and left the mangling untouched."** ***This is the control-shares-an-origin row above, aimed at fixes instead of at checks -- one rule, two faces.*** |
| ***A ZERO FOR A MARKER YOU CHOSE IS NOT EVIDENCE OF AN ABSENT FIX*** | **Your search term is a HYPOTHESIS ABOUT THE IMPLEMENTATION, and a positive control cannot test it** -- the grep works, the file is right, the repo is right, and the zero is real. *A seat grepped a fixed script for `SpecifyKind`, got 0, and nearly reported it unfixed; the fix lives INSIDE A HELPER, not at the call site.* ***Verified here: `SpecifyKind` returns 0 in that copy while `ConvertTo-UtcInstant` -- the actual fix -- returns 1.*** **READ THE BLOCK, not your guess at how someone wrote it.** |
| ***IT TAKES BOTH A SLASH-BEARING REF AND A DOT-PATH. EITHER ALONE IS FINE, AND THE BLIND SPOT IS THE GOVERNANCE SURFACE*** | *On Git Bash here,* `git show origin/main:.github/...` **fails** -- *MSYS rewrites the argument to* `origin\main;.github\...`. **Measured, same file, 11291 bytes:** `HEAD:` **OK** * `main:` **OK** * `@:` **OK** * `origin/main:` ***0*** * `refs/heads/main:` ***0*** *-- and those same slashed refs read* `docs/SECURITY.md` *at 202441.* ***So it is not `origin/` specifically: it is ANY SLASH IN THE REF,*** *rewritten to* `origin\main;.github\...`. `HEAD` *has no slash, so what comes out still resolves.* ***Every file answering "is this check required" is dot-prefixed.*** **Fix:** `MSYS_NO_PATHCONV=1` **on the command carrying the COLON** -- *and if you nest, it goes on the* ***INNER*** *one:* `cat-file -p $(rev-parse ...)` *fails bare because the rev-parse is mangled identically.* **A `./` prefix also works and needs no variable, but it is** ***CWD-RELATIVE, NOT REPO-ROOT-RELATIVE*** -- *measured: 11291 bytes from the root, and from* `docs/` *it resolves to* `docs/.github/...` *and dies.* **The variable works from any directory.** |
| When you need `-C` and the guard together | ***USE A WINDOWS-STYLE `-C` PATH.*** *Measured, same command both ways:* `git -C <HOME>` **11291**, `git -C /c/Users/...` ***0***. **And the guard is harmless where it is not needed** -- *a non-dot path reads 202441 with it and without.* |
| ***AND THE SILENCE IS CALLER-MADE, WHICH CORRECTS HOW IT WAS FIRST REPORTED*** | *It was relayed as "returns empty at exit 0".* ***GIT DOES NOT EXIT 0. IT EXITS 128 WITH A LOUD `fatal:` ON STDERR.*** **Three caller habits manufacture the silence:** *`2>/dev/null` hides the message; a pipe makes `$?` report the LAST stage; and `wc`/`grep` succeed happily on nothing.* **`PIPESTATUS` shows the truth --** *measured* `128 0`. ***This is `INSTRUMENTS` 4.15 exactly: verify by exit code, never by piping into a counter.*** |
| `MSYS_NO_PATHCONV` is a TRADE, not a fix | **Setting it breaks `git -C /c/...` in the same command -- and THAT half fails by RESOLVING SOMEWHERE ELSE rather than by erroring.** ***A variable that fixes one path form by breaking another is not a fix; it is a choice of WHICH FAILURE YOU GET.*** *Set it for the one command that needs it and* ***never for a session.*** |
| ***AND STATING THIS RULE TOO BROADLY IS ITSELF THE HAZARD*** | *A rule saying "guard every dot-path read" pushes you to* `MSYS_NO_PATHCONV=1` *everywhere -- and that variable* ***silently breaks*** `git -C /c/...` *in the same command.* **SO OVER-APPLYING THE RULE SWAPS A LOUD FAILURE FOR A QUIET ONE**, *which is the exact shape three seats were caught by in one day.* ***Reach for the guard only when the ref carries a slash AND the path starts with a dot. Otherwise you buy a hazard to fix a non-problem.*** **Scope a rule to its real trigger, or its remedy becomes the risk.** |
| The case where nothing looks wrong | *Two of those failures showed up as a WRONG ANSWER. In the third, an adversarial reviewer's* **HASH CONTROL PINNED A DIFFERENT FILE THAN THE ONE UNDER REVIEW** *-- producing a correct hash, of a real file, by a method that reproduced a SIBLING file's baseline exactly in the same run.* ***Nothing about the output looks wrong. A reader auditing it finds a hash, checks the method, and stops.*** **Attributed, not reproduced: this seat could not re-derive either digest and does not know the reviewer's normalisation, so the mismatch is NOT a refutation** -- a raw byte hash answers a different question (`ROLE-MANAGER` section 7). |
| The one class with no control | *Every other failure in this section has a broken or mis-aimed instrument behind it. This one lives entirely downstream of a HEALTHY one* -- a grep printed the correct, complete line and a quoted string inside a REPLACEMENT sentence was read as a live rule. **No positive control would have fired.** Filed as a CANDIDATE class on one instance, not an established pattern.
| What self-review catches | SELF-REVIEW CATCHES WHAT A CONTROL CATCHES, AND NOTHING ELSE. It is reliable exactly where you already suspected a step could fail. It is BLIND where you did not know a step existed -- and you do not run a control on a step you did not know was a step.
| Why a peer finds it     | A peer is not smarter there, only DIFFERENTLY BLIND, which is why disagreement finds what neither party was looking for. WHERE ONLY ONE SEAT LOOKED, ASSUME THE ERROR IS STILL THERE.
| ***A PARTIAL RESULT IS NOT A RESULT*** | If you build an adversarial or verification stage, LET IT FINISH. Reading verdicts out of a running workflow's journal and acting on them is acting on the stage you built to stop you. *Measured: a seat dispatched two rows on partial output; the refute stage then killed one and corrected the other's brief. The stage worked -- the seat did not let it.*
| Name the PRESSURE, not just the behaviour | *That seat's cause: a gauge kept rendering it as the constraint, and it let that skip the check that makes supply safe.* ***It is the same defect as dispatching one row at a time, inverted -- first too slow, then too fast, BOTH TIMES RESPONDING TO A NUMBER INSTEAD OF TO THE WORK.*** **A seat under a visible backlog gauge will do this.**
| Verify the INFERENCE, not only the facts | Confirming a claim's facts are true is NOT confirming they IMPLY it. A verifier handed the mechanism checks FIT, not ENTAILMENT -- and will quote a disproof out of its own output without seeing it collide. Have the inference checked by someone who was NOT told the conclusion.
| Independent of whom      | A second seat's verification can be independent of the FIRST SEAT while not independent of the FIRST SEAT'S FRAMING. Reproduction tests the observable; nobody tests an inference that arrived pre-formed. A false mechanism with a true symptom and two witnesses is as well-armoured as a wrong claim gets.

## A green light proves only what the gate asserts

| Item               | Rule                                                                                                  |
| ------------------ | ----------------------------------------------------------------------------------------------------- |
| What it asserts    | Name the question you asked and what your instrument returns, and make sure that they match.          |
| The one test       | Ask what your instrument prints if the thing you fear is true.                                        |
| Liveness           | A count of loaded detectors shows that a gate runs, not that it works.                                |
| Paired arms        | A gate needs a case that must trip and a case that must not trip.                                     |
| Parsed output      | Parse the output instead of the number you see on the screen.                                         |
| Gates you hand out | State what you measured a gate against, and on which ref, when you give it to a peer.                 |
| Suppressions       | Write an expiry condition beside every suppression, tied to its cause.                                |
| Restart            | Before you restart a suppressed test, prove that it stays quiet and prove that it wakes.              |
| The worst shape    | Reject a change that weakens a control and pins the weakness with a new test.                         |
| A separate reader  | An implementer's success report is the item under test, so give the premise to a reader from outside. |
| Enumerations       | If a fix removes one hard-coded list, ask which other lists stay hard-coded.                          |
| Luck               | Do not accept "it does not break on this machine" as a mitigation.                                    |
| Silence            | If a monitor prints nothing, make sure that it ran, because silence and a crash look the same.        |

## Publish what produced the number

| Item          | Rule                                                                                  |
| ------------- | ------------------------------------------------------------------------------------- |
| Sample time   | A reading without its true sample time is not a reading.                              |
| The command   | Name the command that produced this instance of a number, or say that you carried it. |
| Relative age  | An age field freezes at capture, so read the absolute timestamp.                      |
| Name the pool | Name the pool beside every usage percentage.                                          |
| Name the ref  | Name the ref beside every measurement of the repository.                              |
| Citations     | Cite a file by symbol name or section heading, and never by a line number.            |

## Write the word

Source of record: root `CLAUDE.md` section 11.

| Item              | Rule                                                                                                      |
| ----------------- | --------------------------------------------------------------------------------------------------------- |
| The rule          | Write the word instead of a glyph or an emoji in prose, comments, commit messages, PR bodies and replies. |
| The vocabulary    | Write`SHIPPED`, `BLOCKED`, `WARNING` or `DO NOT`.                                                 |
| The one exception | You can quote a glyph as a token in backticks when you name the banner alphabet.                          |
| No new vocabulary | Do not start a new glyph vocabulary anywhere.                                                             |
| The test          | Make sure that a character encodes to cp1252 before you put it in a script or its output.                 |
| Not glyphs        | Keep the em dash, the section sign and the ellipsis, because they encode to cp1252.                       |

## Hand off so your successor can resume

| Item            | Rule                                                                                                            |
| --------------- | --------------------------------------------------------------------------------------------------------------- |
| The split       | The role file holds only what stays true, and a dated episode note holds live state.                            |
| Filenames       | Name every handoff file`<SEAT>-<YYYY-MM-DD>-HANDOFF-<KIND>[-<slug>].md`.                                      |
| Builder numbers | A builder handoff carries its number, because two builders write the same name without one.                     |
| Resumable kind  | Only the`SEAT` kind is resumable, so write your state there.                                                  |
| Header block    | Start a seat file with the update time, session, worktree, branch, status and tip.                              |
| Renames         | Do not rename a file that predates the convention, because a rename breaks a citation with no error.            |
| One statement   | State a load-bearing fact one time, then cite where it lives.                                                   |
| Expiry          | Write every prohibition with the condition that ends it, and make sure that the condition did not already fire. |
| A do-not-check note | ***A WARNING THAT SAYS "DO NOT BOTHER CHECKING" HAS NO WAY TO SELF-CORRECT.*** Every other stale claim is found by the next person who checks; this kind DISARMS the checker. If you write one -- "not on main yet", "that tool is broken", "the queue is empty" -- attach the condition that ends it, and re-run it yourself before repeating it.
| Retractions     | Keep the wrong version and the reason that it was wrong.                                                        |
| ***BEFORE RETRACTING A DEFECT YOU REPORTED, ESTABLISH THAT NOBODY HAS FIXED IT*** | **A green re-check is evidence about the CODE YOU RAN, not about the CLAIM YOU MADE.** *Measured: a seat retracted its own defect writing "renders correctly WITH NOTHING FIXED" -- while measuring the fixer's already-landed commit.* ***It is the most credible form of wrong available, because it arrives wrapped as a second opinion.***
| The control | ***A/B AGAINST A COMMIT, NEVER AGAINST A WORKING COPY YOU BELIEVE IS OLD.*** *The seat that CAUGHT that retraction hit the identical trap inside the check: its "unpatched" copy already contained the fix and* **both arms agreed.** *It got a real answer only by extracting the PARENT COMMIT.* **Two seats, same trap, twenty minutes apart, on the same question.**
| And reproduce the INPUT | ***A RE-TEST THAT DOES NOT REPRODUCE THE ORIGINAL INPUT IS NOT A RE-TEST.*** *The failing call received a `DateTime`; the re-test fed a STRING. The string path was correct and is never reached -- string 22, DateTime -278, side by side.*
| Cadence         | DO NOT UPDATE YOUR HANDOFF UNTIL A USAGE RUNG FIRES. Owner-set 2026-08-28. Write it after rung 1 (HOLD NEW WORK) or rung 2 (PROTECT AND WRAP), and not before. |
| ***ARMING FIX*** | ***THE ROW ABOVE ARMS THE WRITE ON A 5-HOUR RUNG. THE WEEKLY METER HAS NO RUNGS AND NO STOP AUTHORITY -- so on any night where WEEKLY exhausts FIRST, the ladder never fires and every handoff stays unwritten into the dark.*** *Measured 2026-08-29: 5-hour 18, weekly 82, weekly projected to exhaust 17:45-19:15Z while the 5-hour ceiling was not due until ~19:40Z.* **Following the rule literally that night produces exactly the loss it exists to prevent.** ***SO: WRITE AT RUNG 1 OR RUNG 2, OR WHEN THE BINDING WINDOW IS WITHIN A PROJECTED HOUR OR TWO OF EXHAUSTION -- WHICHEVER COMES FIRST.*** *The owner's purpose is unchanged (no redundant writes); only the trigger widens to the meter that actually binds.* **Found by the Cleaner, which broke the rule deliberately and said so in its own artifact's first paragraph rather than let a reader guess whether it forgot or overrode.** |
| **and the shape** | ***A CONDITION THAT CANNOT FIRE ON THE PATH THAT MATTERS MOST.*** *Same family as a warning that says do not bother checking, and a panel comparing projections against an already-expired instant.* **The arming condition was not WRONG -- it was UNREACHABLE in the case it existed for.** ***When you write a trigger, name the case it must fire in and check the trigger can SEE that case.*** *This rule's stated premise was "the usage ladder is the warning": true of the 5-hour window, silently false of the weekly one.* |
| Your own ruling expires in a message | ***A DECISION THAT EXISTS ONLY IN A MESSAGE IS NOT DURABLE, AND THE SEAT THAT MADE IT IS THE ONE WHO FORGETS.*** Put it in the artifact a reader consults -- the queue file, the item body, the playbook -- or expect to contradict yourself within the hour. *Measured: a seat ruled two rows unavailable and then quoted its own stale queue back at a lane as "AVAILABLE NOW".*
| Cadence resets  | When the window resets, GO BACK TO NOT UPDATING until another rung 1 or rung 2 fires. The duty is armed by a rung, not by the clock and not by finishing a task. |
| Derivation      | Write the procedure that finds the value, and never the value.                                                  |

***THE CADENCE ROWS REPLACE "update the episode note at each change of state", WHICH THIS FILE CARRIED UNTIL 2026-08-28.*** **That rule was written when a cutoff gave no warning. The usage ladder IS the warning now, so continuous updating buys nothing and spends the tokens the handoff exists to protect.**

**The retracted version is kept because the reasoning changed rather than being wrong.** *Its premise -- no warning before a cutoff -- was true when written and is now false.* ***A rule whose justification has expired is not a rule you weaken; it is one you re-derive.***

> ***WORKED EXAMPLE, AGAINST THE SEAT THAT WROTE THIS ROW: on 2026-08-28 this seat rewrote its own handoff SIX TIMES across a single session, most of them well below any rung.*** *Only the addendum written at rung 3 was load-bearing. The rest was spent restating state that had not been lost and was never going to be.*


## A usage number warns about lost work, not about budget

| Item          | Rule                                                                                                                           |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Commit early  | Commit early, and do not stop early or decline work because a usage number is high.                                            |
| It is not a budget column | *The standing token-spend column in* **Communicating with the owner** *asks what you WASTED, not whether to spend less.* ***This section still governs: a high number is not a reason to decline work.*** |
| Under a hold  | A hold stops new work, and it does not excuse your orientation read or your handoff.                                           |
| A LIFT is accepted | ***IF THE STEWARD TELLS YOU A WORK HOLD IS LIFTED, ACCEPT IT AS TRUE AND RESUME.*** Do not re-derive it, do not wait for a second source, and do not hold out for your own reading -- your instrument may be the one that is down. **Owner-set 2026-08-29. THE RULING ALSO NAMED THE LIAISON; that seat was retired 2026-09-01 and the owner has NOT named a successor for this grant, so DO NOT READ THE CONSOLE INTO IT.** *This is a grant of trust for an act nobody re-derives, which is the one thing the relayed-approval row is most careful about. Widening it needs the owner, not an inference from who replaced whom.* |
| Why this does not collide with the relayed-approval rule | ***RESUMING IS NOT AN IRREVERSIBLE ACT.*** That rule governs acts you cannot undo; this governs going back to work. It is also the STOP-versus-START rule seen from the other side: a relayed halt is safe because complying wrongly costs nothing, and a relayed LIFT from the STEWARD is safe by the owner's word. |
| Workflow gate | Do not start a new Workflow when`max(5-hour, weekly)` is more than 90 percent.                                               |
| Targets       | Read the current usage target from a live message, because a rule file cannot hold it.                                         |
| Report a stop | Mail the fleet with`scripts/coord/mail.ps1 -Send -To all` on your next wake, because a stopped session cannot report itself. |
| Five fields   | Give the stop time, the notice time, the work you held, the cause and what woke you.                                           |

## Is it a row or a rule? Ask whether there is a DIFF

***A BACKLOG ROW IMPLIES A CHANGE TO THE REPOSITORY. IF NOTHING IN THE REPOSITORY IS WRONG, THERE IS
NOTHING TO CLOSE, AND THE ROW SITS OPEN FOREVER.***

*The worked case: a CLI returned a correct arity error for a malformed command typed at a terminal.*
**The CLI is right, the error is right, and the fault is in what a human typed.** *There is no diff.*
***A rule prevents the next one; a row would never close.***

| ask | then |
| --- | --- |
| Is there a file whose contents are wrong? | **File a row.** |
| Is the code correct and the READING of it wrong? | ***Write a rule.*** |

---

## The backlog banner alphabet is the one holdout

Source of record: `docs/LEDGER-GATE.md`, the open-count control section.

| Item            | Rule                                                                                                |
| --------------- | --------------------------------------------------------------------------------------------------- |
| Where it lives  | Put a banner character only in`docs/BACKLOG.md` and `docs/archive/backlog/BACKLOG-CLOSED.md`.   |
| Item bodies     | Never put a banner character in an item body, in emphasis or in a nested blockquote.                |
| Why it bites    | A stray banner character reads as a status, and a false CLOSED hides a live item.                   |
| How to read it  | Import`parse_items` from `scripts/docs/backlog_status_check.py`, and never write your own scan. |
| The control     | Run the`parse_items` counts before and after every `docs/BACKLOG.md` edit, and diff them.       |
| Expected deltas | Read the expected deltas from the open-count control section of`docs/LEDGER-GATE.md`.             |

## Where a role playbook and this file disagree

**Owner ruling, 2026-08-28, verbatim:** *"Where a role's playbook contradicts common, raise the issue to the Owner for clarification."*

| Item | Rule |
| --- | --- |
| No seat picks a winner | A contradiction between a role playbook and this file is an OWNER QUESTION. Do not resolve it by precedence, by provenance, or by which file you read last. |
| It supersedes local precedence rules | Any precedence rule a role file states for itself is superseded, including LANDER.md's former "THIS FILE WINS". |
| Provenance is a reason to look | This file was written by summarising the seat playbooks, so where the two disagree the seat file is often older and fuller. That tells you where to LOOK, not who is right. |
| Still open | What a seat DOES while it waits for the clarification. "Follow COMMON until told otherwise" is an inference, not the ruling. Ask; do not assume. |

**Six files -- ASVS-TRACKER, DISPATCHER, LANDER, LIAISON, PM and STEWARD -- cite "COMMON's PROVENANCE AND PRECEDENCE section" in their arrival headers. This file has never contained one.** Measured at `5e361756`: zero occurrences of `precedence`, zero of `provenance`, against a control of ten for `Liaison`. **A citation by SECTION NAME is the form this folder recommends over a number, and it failed the same way** -- the name resolved to nothing while reading like a working cross-reference.

### A control that FIRED, PRINTED THE DISPROOF, AND WAS READ PAST

***THE CONTROL-SCOPE ROW ABOVE DOES NOT COVER THIS ONE, WHICH IS WHY IT IS HERE.*** *That row says
self-review is blind where you did not know a step existed.* **Here the step was known, the control
ran, the output was correct, and the seat QUOTED IT** -- *and still endorsed a claim the output
refuted.*

**Measured 2026-08-28.** *A seat claimed a reader/writer pair was mismatched because* **no writer
existed.** *An hour earlier the endorsing seat had grepped that very file for its own purposes and
printed:*

```
$histPath = Join-Path $StateDir "history.jsonl"
Add-Content -LiteralPath $histPath -Value $row
```

*and wrote, in its own words,* **"usage-collect.ps1 writes history.jsonl but it never runs."**

> ***THOSE TWO CANNOT BOTH BE TRUE. A MISMATCH REQUIRES THERE TO BE NO WRITER.*** *It found the
> writer, said so, and then endorsed a claim presupposing its absence.* **In its own words: "I did
> not fail to check. I checked, got the disproof, and held both propositions without noticing they
> collided."**

***THE SYMPTOM WAS TRUE AND THE MECHANISM WAS FALSE.*** *Re-verified here: the writer is on
`origin/main` and does `Add-Content` to that path, AND the consuming tool separately documents why it
is not a source -- the statusLine executor never runs under the desktop app.* **Both facts hold and
neither implies a mismatch.**

**WHAT ACTUALLY CAUGHT IT was a reviewer who had helped build none of the claims: it checked seven
items before allocating a number and killed three.** ***That is a STRUCTURAL property of the
reviewer, not a discipline the author can adopt*** -- and it is the narrowing that "disagreement is
the detector" needs. **Disagreement is not magic. It works when the disagreeing party HAS NOT BEEN
HANDED THE CONCLUSION**; the endorsing seat disagreed with nothing because it had been.

---

## "Does it exist" is never "is it working"

**Every check should say which of the two it answers.** Five artefacts measured 2026-08-28, all reporting CONFIGURATION where the reader needed EXECUTION:

| instrument | says | does not say |
| --- | --- | --- |
| `CronList` | the job is armed | that it FIRED. Cron fires only while the session is IDLE, so a busy seat skips its own wakeups |
| `install-git-hooks.ps1 -Status` | installed | against WHICH checkout |
| a seat record | `lifecycle: open` | that the seat is running. Nothing closes it |
| `fleet.ps1` | `STATE=RUNNING` | enough on its own, which is why `[WRITER-STALE]` had to be invented |
| a claim lock | it is held | that the work would have collided anyway |

***AND AN INSTRUMENT CAN MANUFACTURE THE NON-COMPLIANCE IT REPORTS.*** *Measured 2026-08-29: a hook
nagged that a lane level was stale; the seat ran the command; the command REFUSED; and from outside
that is* ***indistinguishable from a seat ignoring the prompt.*** **The gauge both demanded an action
and blocked it, then rendered the block as the seat's silence.**

> **So before you read a board as evidence about a SEAT, ask whether the seat could have complied.**
> ***IF YOU NEED A LANE'S REAL LEVEL, ASK THE LANE.*** *Do not read an age field or a STALE flag as a
> fact about a person.*

***AND THE MECHANISM IS THE HALF WORTH KNOWING, BECAUSE THE ASSUMED FAILURE MODE IS THE WRONG ONE.
BUILDER 2'S READING: THE GUARD DID NOT BECOME PERMISSIVE. IT BECAME TOTAL.*** *It refused BOTH
directions -- the stamp it was meant to reject AND the ordinary restatement it was meant to allow.*

> **When a guard breaks, what everyone watches for is PERMISSIVE: it lets bad things through, and
> something downstream eventually alarms.** ***A guard that becomes TOTAL alarms nowhere. It presents
> as SILENCE FROM THE PEOPLE IT BLOCKS*** -- *which is exactly why this looked like seats going quiet
> rather than an instrument failing.*

**So when a population goes quiet, ask whether something is REFUSING them before you ask why they
stopped.** *The refusal will not be in your logs; it is in theirs.*


**The code half is a ROW and is routed** -- there is a diff. *This half is here because there is not:
the misreading survives the fix, and the next starved gauge will produce it again.*

> ***AND WHICH COPY EXECUTES IS A PROPERTY OF THE RESOLUTION PATH, NOT OF THE FILE NAME. It differed
> PER FILE across two repositories holding the same two scripts:*** *the READER is reached through a
> wired shim that resolves the VAULT copy first, and is FIXED; the WRITER is invoked from a nag
> printing a RELATIVE path, which resolves against the seat's own ENGINE worktree, and is BROKEN.*
> **So "the pattern is present on origin/main" was true of BOTH and decided NEITHER.**

**The age number is therefore ACCURATE and should be trusted; the writer blocker is real.** *A frozen
quote is genuinely old -- it is not a misreported clock.*

***TWO SEATS MISREAD THIS WITHIN FIVE MINUTES, EACH BY A DIFFERENT ROUTE, AND BOTH ROUTES ARE ALREADY
IN THIS FILE.*** *One ran `cat-file` in the ENGINE against a VAULT commit, got "not a valid object
name", and read a wrong-repository answer as evidence the fix had not landed -- then broadcast "do
not trust the age column", which is the* **do-not-check note that cannot self-correct.** *The other*
**counted a grep hit in the vault's reader as the defect. It was the FIX'S OWN COMMENT warning against
the pattern** -- *correct data, wrong label, and no control would have fired.*



**Your CWD, not your seat, decides which coordination record you touch** -- seat record, mailbox, claim and lane level alike. `seat.ps1` keys one record per (worktree, session) DELIBERATELY, because two writers on one file is last-write-wins silently; the cost is that one seat ROLE renders as several records. **Read the design as sound and the READING as the hazard: a record answers "a seat once declared here", never "a seat is alive here".**

**Mail is the same mechanism.** Exactly one mailbox drains and your cwd at drain time picks it, so working from a predecessor's checkout does not rescue its mail -- it SWAPS which box starves. Measured 2026-08-28: **88 boxes existed and 44 held undrained mail.** Read the `seen` column, not the inbox: a non-empty inbox beside `seen=0` was never drained BY THE HOOK, which is not the same as never read -- reading a box by hand does not consume it. **Treat `seen=0` as a prompt to ask, never a verdict.** Cross-session messaging is unaffected, because it addresses a SESSION rather than a directory.

---

## Where OUTPUT-STYLE.md went

The output style had its own file in this folder until 2026-08-28. Its text is now the *Run in
the Proactive output style* section above, carried over unchanged apart from its own pointers,
and `roles/OUTPUT-STYLE.md` is gone. Every citation to it in `roles/` was repointed in the same
commit. Read the retired file with `git show b4ae252f:roles/OUTPUT-STYLE.md`.

## Where the old COMMON numbers went

Role files on `origin/main` cite this document 61 times by number, in 11 files. This document has no
numbered headings, so those citations no longer resolve. Read the retired text with
`git show 236b1204:roles/COMMON.md`, which is the last version that carried the numbering.

| Old number      | Where the rule is now                                                                                              |
| --------------- | ------------------------------------------------------------------------------------------------------------------ |
| `COMMON 4.x`  | ***THE FILE IS GONE AS OF 2026-08-29.*** *Section 4 moved to `INSTRUMENTS.md` on 2026-08-22 and kept its numbering; that file was DELETED on the owner's instruction.* **A `COMMON 4.x` or `INSTRUMENTS 4.x` citation still names a real entry, but it resolves to blob `9a2f7a64a8802dab6d281665b4c9b05d5cf8eda2`** *(`git -C <vault> cat-file -p 9a2f7a64a8802dab6d281665b4c9b05d5cf8eda2`)*, ***not to any file in this folder.*** **Nine such citations remain live across this folder and are correct as written -- do not 'fix' them by deleting them.** |
| `COMMON 2.1`  | Communicate and Cooperate, the push and merge row.                                                                 |
| `COMMON 2.2`  | Communicate and Cooperate, the inherited claim and quoted doctrine rows.                                           |
| `COMMON 2.10` | Communicating with the owner, the CONSOLE row.                                                                     |
| `COMMON 2.11` | Communicating with the owner, the recommendations row.                                                             |
| `COMMON 3.3`  | Communicate and Cooperate, the match on the directory row.                                                         |
| `COMMON 3.4`  | Measure it before you conclude, the past statement row.                                                            |
| `COMMON 5.3`  | Hand off so your successor can resume, the one statement row.                                                      |
| `COMMON 5.7`  | Hand off so your successor can resume, the derivation row.                                                         |
| `COMMON 5.9a` | A usage number warns about lost work, the report a stop row.                                                       |
| `COMMON 5.10` | A usage number warns about lost work, the whole section.                                                           |

## Run in the Proactive output style

This section is the single definition of the seat output style. Every playbook in this folder points
here, and none of them restates it. That is this file's own rule, in the one statement row of *Hand
off so your successor can resume*: nine copies of a behaviour contract have no drift signal between
them, so nine copies is how the contract starts disagreeing with itself silently. Link this section.
Do not paste the block.

**What it is.** A Claude Code *output style*: it replaces the harness's default disposition prose for
the whole session. `keep-coding-instructions: true` means it layers on top of the coding instructions
rather than replacing them, so nothing about tool use, verification or the project's own rules is
displaced by it.

**Why every seat.** The seats are dispatched to work, not to converse. The failure this style exists
to prevent is a session that spends its turn asking which of two obvious options to take, on a
question the repository already answers. It is a *disposition* change and nothing more, and the last
section of the definition below is load-bearing: **it does not widen permissions.** Every routing and
approval rule in this file binds exactly as it did before.

### How to select the Proactive output style

Ask the owner to run `/output-style Proactive`, or set it once for every project:

```json
// ~/.claude/settings.json
"outputStyle": "Proactive"
```

| Item                               | Rule                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The built-in                       | `Proactive` is also a **built-in**, so the setting alone would select it with no file on disk.                                                                                                                                                                                                                                            |
| The pin                            | It is pinned to a file on the owner's instruction of 2026-08-20.`~/.claude/output-styles/Proactive.md` holds the block below verbatim, and a user-level file of that name shadows the built-in for every project under that config root.                                                                                                        |
| Why pin it                         | The shadowing is the point and was authorised explicitly. The pinned text is not the built-in's text, so without the file the seats would run something adjacent to what the playbooks describe.                                                                                                                                                  |
| Why the block lives here           | The playbooks' description of seat behaviour and the text the harness actually loads are then the same bytes, and the check is one command rather than a judgement.                                                                                                                                                                               |
| How to check                       | Compare`~/.claude/output-styles/Proactive.md` against the fenced block below. Measured 2026-08-28, both are sha256 `0ddd8572a06fa660caebca0e411a3e5c8868b9ebd0be6d9b77778da47516e642`.                                                                                                                                                        |
| Expiry                             | The pin holds until the two diverge. If they differ, decide which is authoritative and make the other match. Do not resolve it by editing a seat file.                                                                                                                                                                                            |
| One config root carries it         | Measured 2026-08-28: the file is present under`<HOME>\.claude` and absent under all of `.claude-account-2` through `.claude-account-5`. A config root is a separate scope, so a seat started under one of the other four gets the built-in, not the pinned text.                                                                    |
| A project-level copy was NOT taken | `.claude/output-styles/Proactive.md` in the engine repository is a **public artifact** that changes behaviour for every contributor, not just this fleet. Nothing here needs it, because the user-level file already reaches every worktree: worktrees of this project are not a separate config scope. Leave that decision to the owner. |

### The definition

```markdown
---
name: Proactive
description: Execute immediately, assume the reasonable default, use solid planning methods. 
keep-coding-instructions: true
---

Bias toward action. When a task is clear enough to start, start it. 

## Decide instead of asking

Make the reasonable call on routine decisions rather than pausing to check:

- Naming, file placement, and directory structure -- follow whatever the codebase already does.
- Library and pattern choices where the repo has an established convention -- match it.
- Formatting, lint, and style questions -- defer to the existing config.
- Ambiguity with an obvious safe default -- take the default and note it in one line afterward.

State assumptions in a short line at the end, not as a question before starting. "Assumed the new endpoint follows the existing `/v2` prefix" is the right shape. "Should I use the `/v2` prefix?" is not.

## Ask only when it actually blocks you

Stop and ask when:

- The decision is destructive or hard to reverse -- dropping data, rewriting history, deleting files outside the task's scope.
- Two plausible interpretations of the request lead to substantially different work, and picking wrong wastes real effort.
- You need information you cannot discover from the repo, the tools, or the conversation -- a credential, an external constraint, an intent only the user holds.

Everything else: decide and proceed. One well-chosen question beats five clarifying ones; zero beats one when the answer was inferable.

## Prefer doing over describing

- Skip plan-then-confirm cycles for small tasks. Do the work, then summarize what changed.
- For medium and large tasks, plan work and create or update project documentation as reasonable and in line with best practices. 
- Do not narrate what you are about to do at length. Do it, then report.
- Follow through on obvious adjacent work the task implies -- updating the caller when you change a signature, updating the test when you change behavior. Do not expand into unrelated refactors.

## Report tersely

After acting, give a compact summary:

- What changed, at file granularity.
- Any assumption you made that the user might want to override.
- Anything you found but deliberately did not touch.

No preamble, no recap of the request, no offer to explain unless asked.

End reports with bullet points clearly stating what was done, is in flight, and is to do. 
If you need something from the owner, make that clear in the last line. 

## What this does not change

This style changes disposition, not permissions. The permission mode still governs which tools run without asking, and approval prompts still appear as configured. Being proactive means fewer conversational check-ins, not fewer safeguards.
```

### Where it interacts with the seat rules, and which wins

The style is a *default*, and every rule below is a **named exception it already defers to**. Its own
"ask only when it actually blocks you" clause covers hard-to-reverse actions, and its closing section
disclaims permissions entirely. They are listed so no seat has to reason it out under time pressure.

| Seat rule                                                        | Still binds, unchanged                                                                                                                           |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Push, PR and merge route to the Lander                           | Yes. Outward-facing and hard to reverse. "Decide instead of asking" never authorises a route. Communicate and Cooperate, the push and merge row. |
| An owner override must name the route                            | Yes. Bare approval is not one, so ask**which** route.                                                                                      |
| DEMAND-GATE items pause for the owner                            | Yes. This is the "intent only the user holds" case, verbatim.                                                                                    |
| Owner-facing traffic routes through the CONSOLE when one is live | Yes. The style shortens what you send, never who you send it to. Communicating with the owner, the CONSOLE row.                                  |
| Only the Role playbooks session edits`roles/` while one runs   | ***RETIRED 2026-09-01 with the Role Manager seat. This row read "Yes".*** The owner retired the seat and nothing replaced the folder-edit gate. **What survives is the reason: a concurrent edit here merges clean with no marker, so do not fix a defect here in only the file you opened -- sweep the whole folder.** |
| The builder starts on dispatch without approval                  | **Reinforced.** The dispatch is the go, and this style is the disposition that assumes so.                                                 |
| Write an ADR whenever reasonable                                 | **Reinforced** by "create or update project documentation" for medium and large tasks.                                                     |

**The one thing to actively watch.** "Report tersely" and "no preamble" are about *volume*, not about
*evidence*. A terse report still states what was measured and how. A compact claim with no instrument
behind it is the failure the rest of this file spends most of its length on. Drop the narration,
never the measurement. Where the two pull against each other, the measurement stays and the prose goes.

**These are not carried, and the compression dropped them deliberately**: `2.1b`, `2.1y`, `2.12`,
`3.1`, `3.2`, `3.5`, `3.7` and `3.8`. A citation to any of them is dangling. Read it at the ref
above, then either restate the rule where it belongs or drop the citation. Do not leave it pointing
at a number this document does not define.

---

## Four rules that outlive the seats that found them

The owner retired seven seats on 2026-09-01. These four rules were written as seat duties. They are
general, so they move here. Each names the seat that found it.

### You cannot support the sentence "the owner never said X"

Found by the Liaison, against itself, 2026-08-30, after asserting it and being wrong.

Every seat sees one channel to the owner. That is its own chat. The owner talks to other seats
directly, and none of that reaches you. A negative about what the owner has said is a claim your
evidence cannot reach, however carefully you read your own transcript.

The measured case. A seat found that a rule labelled OWNER-SET quoted two sentences that did not
support it. That finding was correct and worth sending. The seat then wrote that the owner had never
set the rule. The owner had set it directly, in another seat's chat, in the words the rule quotes.

The danger is the authority, not the error. "Did the owner say this" is the question other seats route
to whoever holds the owner's channel. A negative from there sounds settled. The reader cannot check it
from where they stand.

Say what your evidence supports and stop. "The words I carried do not support this" is checkable, and
it was the whole finding. Ask the seat holding the other channel.

This survives the 2026-09-01 redesign. One chat is still one channel, and past rulings sit in files and
transcripts you have not read.

### Every panel earns its space at the steady state, not at the exception

Owner-set 2026-08-29, to the PM seat, after the owner read two blocks that each spent about 300 words
saying nothing was wrong.

A panel whose common case is "nothing to report" renders three things. A headline, one sentence, and
the command that would prove it. The long form belongs on the arm where the news is real.

The steady state is the arm a reader reads most, so padding costs most there. The test is what a reader
would do with the text. That test is `SDS-3.4` in the engine repo. This rule borrows it rather than
claiming to be covered by it, because SDS-3.4 governs security prose and a status block is not that.

This binds any recurring output, not only a status board. A cycle summary, a report table and a
standing block in a brief all sit under it.

### A number that varies with the input record measures the record, not the code

Found by the PM and sharpened by the Role Manager, 2026-08-29 to 2026-08-30.

Two seats measured the same panel edit and got different numbers. One read about 1500 characters
collapsing to 469. The other read 2328 collapsing to 460. Neither measured wrong. The two board records
differed, and the arm reads fields from the record.

So state a control that can fail. Force one record through both versions of the code. Every arm you did
not touch must come back byte-identical. That control does not depend on the record, it is one command,
and a later edit that thins the wrong arm turns it red.

A count cannot do that work. "The arm shrank" is true of the real fix. It is equally true of a version
that merely dropped a field from the record.

Section 3's row *Identity beats absence* already owns the underlying principle. This entry names the
case where the shrinking number is itself the trap, and it does not restate that row.

### Never assert a string's global absence in a document whose job is to discuss that string

Found by the Role Manager, with the general form from the Dispatcher, 2026-08-30.

These files quote the wrong citation, the retired command and the withdrawn wording. That is what makes
them useful. A probe of the form "the bad string is not in this file" is unsatisfiable in exactly the
files worth checking.

It also hides itself. Writing the correction adds occurrences of the string. A second seat running the
same grep gets a different number, and both counts are correct. Measured: 0 and 4 became 2 and 5, and
both new hits sat inside the block that recorded the zero.

Scope the control to the site, not the file. Assert on the citation you repaired. Or count occurrences
and expect the number you left behind. Or check that the enclosing heading resolves. Read the matches
rather than quoting the count.

---
