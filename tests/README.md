# Tests

```
python -m unittest discover -s tests -v
```

They are plain `unittest` classes, so no test runner is required. `python -m pytest tests -q` works
too where pytest is present; both runners were measured against this suite and both pass it. (No
count is quoted here on purpose -- one was, and it was stale within a day. The runner prints it.)
CI uses the unittest form, run from inside `tests/` as
`python -m unittest discover -s . -p 'test_*.py'` -- that and `discover -s tests` from the
repository root find the same set.

**Python 3 standard library, plus one external tool: `pandoc` must be on `PATH`.**

`test_word_copy_tracks_the_markdown.py` rebuilds `docs/word/KORUS.docx` from `docs/KORUS.md` and
compares the two, so the converter is a dependency of this suite rather than an optional extra. That
file FAILS rather than skips when pandoc is absent, because the artifact it guards is published and
a skip prints beside the passes.

Prefer the version the committed copy was built with. CI installs **pandoc 3.10**, pinned by sha256
in `.github/workflows/gates.yml`. A different one can report drift that is not there, and the fix
for that is not to edit the markdown.

**This requirement left once and came back**, which is why the sentence above is worth reading twice
if you remember the old one. It went to
[secure-development-standards](https://github.com/wshallwshall/secure-development-standards) with
the Word copies at the split -- along with the rule-identifier, heading-citation and selector-parity
checks, which pinned documents this repository no longer holds -- and returned on 2026-08-13, when
`docs/KORUS.md` began offering a Word download of its own.

Nothing here is installed by the tests, nothing is committed, and nothing outside a temporary
directory is written.

---

## What is here, and why each one exists

| File | Pins | The failure it exists for |
|---|---|---|
| `test_install_gate_wiring.py` | every tool the gate branches on appears in the installer's matcher list, and no matcher names a tool the gate ignores | a `PreToolUse` rule is only invoked for the tools its matcher names. A rule whose tool appears in no matcher **never fires**, and nothing says so: the script is present, it hashes correctly, and every test that runs it directly still passes -- because those tests bypass the matcher that decides whether it runs at all |
| `test_worktree_gate_no_args.py` | the gate, run with **no arguments**, is off with no allowlist, denies a write into a governed root, and allows one outside | the allowlist path is a **parameter default**, and a default is not evaluated when the caller supplies a value. A suite in which every case passed `-ReposFile` explicitly never executed that line once -- and a version shipped whose default died before the script's first line, with the suite green throughout |
| `test_installers_never_write_pre_commit.py` | no installer this repo ships -- all four are enumerated -- writes, moves or removes `pre-commit`, and the git-hook installer still *reports* on it | two tools cannot both own that file. A hook framework that finds a foreign hook there may rename it and drive it from its own shim; that chain has failed on Windows and blocked every commit in a repository until the shim was removed. The renamed file *existing* did not indicate success -- only a real commit did |
| `test_installer_copy_lists.py` | each installer carries its control's dependency closure: the module the Python checkers import, the helpers the gate dot-sources | a checker installed without the module it imports raises at import and refuses **every** commit; a gate installed without its helpers exits 0 after a stderr receipt and enforces **nothing**. Both look installed, and both hash correctly |
| `test_deny_reason_carries_provenance.py` | both the deny reason and the receipt log carry a **digest of the gate file that ran**, and the digest tracks CONTENT -- mutate a copy and it moves, while the hand-bumped version label stays put | the gate is installed by copying it out of whatever checkout the installer happened to run in, so "the installed gate" and "the gate you are reading" are routinely different files, and nothing on the machine said so. The only marker was a string bumped by hand, and it has already lied: three rules were added without a bump, so `-Status` printed the same version on both sides directly above a `*** STALE ***` verdict. A stamp reading `sha ...` that never moves is a second label wearing a hash's name |
| `test_deny_reasons_fold_caller_values.py` | every deny site that interpolates a caller-controlled value folds it first, and the fold neutralizes both known payload shapes **when actually executed** | a deny reason is not a log line. It is an INSTRUCTION an agent reads and acts on, and several carry a literal command block. Two interpolated values are attacker-influenced: `git check-ref-format` accepts `;`, `$`, `\|` and quotes inside a branch name, and an embedded newline in a target path forges a SECOND "What to do instead:" block -- placed first, a model reading top-down reaches the forged command before the real one. Both were found in the project this tooling came from, one from each end |
| `test_gate_remediation_is_runnable.py` | the branch-reuse remediation passes a git ref to `-Branch` and a directory slug to `-Name`, and `new.ps1` actually declares `-Branch` | the remediation used to interpolate the target ref into `new.ps1 -Name`, which is a directory component and cannot contain `/` -- so the printed command was rejected by the very script it named, and it failed for exactly the namespaced ref the rule fires on most. Note the shape: the gate worked, the denial was correct, and the advice was unusable. Nothing failed loudly. The agent is told what to do at the one moment it is being stopped from doing damage, and the instruction does not work |
| `test_docs_do_not_drift.py` | the install block still exists where the pattern can see it and README carries no second copy that disagrees; every same-origin source URL names a path the built site actually serves, and no link has reverted to the GitHub form, and no link into the sibling documentation site names one of its headings with a `#fragment`; no copy describes the installer refusal more loosely than the installers implement it; no page spells a summary heading a way the set replaced; and `docs/HOUSE-STYLE.md` declares a next-free rule number for every series it issues, each above everything that page already uses; and no copy of the write-collision measurement -- including the four that leave this repository, in `CLAUDE.md.template` and three PowerShell scripts -- states a share of writes without naming the population it is a share OF, while README.md still carries the raw counts | six ways this set goes wrong with nobody noticing. The procedure can exist in more than one copy, and did -- one page said the installers refuse when `$env:CLAUDECODE` "is set" while the code tests `1`, which is looser than the code and wrong for `CLAUDECODE=0`. Links to files outside `docs/` had to become absolute, which converts an in-repo reference that a move breaks LOUDLY into an external one that a move breaks silently -- and the host then moved: those URLs pointed at github.com until 2026-08-10, when the owner confirmed the firewall in front of this project's readers blocks GitHub wholesale and not merely the Pages host, making all 35 of them unreachable for the audience the documents are for. They now name the copy the site publishes itself, and a regression to the GitHub form is banned by pattern because it looks correct to whoever writes it -- they are reading the page they copied it from. And a doc claim about a control can outlive the control: the `CLAUDECODE` sentence was not wrong when written, it was wrong about what the code tests A cross-repository anchor is banned for a different reason and it is the sharper one: it is the single link shape NOTHING can verify, because this suite resolves anchors by reading the target's headings and cannot read another repository's, while theirs cannot read ours. A rename on either side leaves a link that renders, clicks, returns 200 and lands at the top of the right page, reported by nobody. Two existed on 2026-08-10, both pointing from the sibling repository into this one, both resolving by luck. A cross-repository CHECKER was rejected rather than overlooked: it would have to fetch over a network this suite never touches, and it could not prevent the break anyway, since a rename here breaks their links the moment it merges. The by-name form it pushes to is not checked either -- what changes is that its failure is visible to a reader. The summary heading is the fourth, and what is pinned now is only its SPELLING. The convention drifted three times in one evening and every drift was green: the set spelled it `## In short` on some pages and `**TL;DR --**` inline on two, the owner settled it as `## TLDR/BLUF`, and three pages gained a summary the old way while that rename was in flight, none of them wrong to do so because nothing recorded that the spelling had moved. Requiring the section was a separate rule with a separate life: OPEN-2 gated its presence from 2026-08-10, when 15 of the 18 rendered pages carried none, until 2026-08-27, when the owner retired it along with OPEN-1, OPEN-4 and OPEN-7, which described what went inside it. Both halves of that gate are deleted, and the second outlived its rule by a day -- the three-label half went on 2026-08-16 with OPEN-7 and its assertions the day after, so for one day the rule sheet described a gate it no longer had while the gate went on running. A semantic check needs a reader. A ban on two dead spellings does not, which is why it is the half that survives. The numbering is the fifth, and it is half a check on purpose: the rule identifiers are ONE namespace shared with secure-development-standards, whose sheet split from this one and kept counting, so a number this page does not use is occupied rather than free. The sibling half cannot be checked without fetching another repository over a network this suite never touches -- the same judgment that banned a cross-repository anchor CHECKER rather than building one -- so that column stays a dated manual reading and the page prints the commit it was read at. What IS checked is the half that actually rots: a rule added here without bumping the declared next-free number, handing the next session a number this page already used. Neither side can see the other move, and both have been wrong about it: the sibling states this page issues OPEN-1 to OPEN-6, which stopped being true when OPEN-7 landed. The denominator is the sixth, and it is the only one here that was never a drift: README.md scoped the write-collision figure to sessions seated in the shared primary and docs/TIPS-AND-TRICKS.md stated it over all file writes, both from the root commit onward, so there is no before-state to bisect to and no edit to blame. It survived the repository's whole eight-day life because each sentence reads correctly alone. The ban is on the loose form rather than a requirement for one spelling, for the reason `TheBlufConventionHasOneSpelling` records, and the empty-match guard sits on README.md because a guard on a pointing file fails the moment the pointing works |
| `test_internal_links_resolve.py` | every relative markdown link names a file that exists, every `#anchor` names a heading in it, and every link written as one of **this site's own served URLs** resolves back to the markdown behind it | these pages cross-reference each other constantly, and a link into a reworded section **does not error**. It renders, it navigates, and it silently lands the reader at the top of the page instead of at the section the link text promised -- so the failure is invisible from both ends. A rename breaks the same links the same quiet way, and `STANDARDS-LANDSCAPE.md` was split in two while this suite stayed green throughout. The served-URL form fell between two checkers: it starts with `https://` so the relative scan skipped it, and it is not a `blob/main` or `raw.githubusercontent` URL so `test_docs_do_not_drift.py` never saw it either. Those links are deliberate -- `WHICH-STANDARDS-APPLY.md` writes its third column the long way because a relative link is dead in the Word copy -- so they had to be checked rather than rewritten. It also **bans an anchor into any heading containing `--`**, which is the one shape github.com and the published site slug differently: github keeps both literal hyphens and turns the spaces around them into two more, kramdown folds `--` into an en dash and drops it. Measured on the built site, 766 headings compared, 19 disagree and every one is a `--` heading. This file computes github's rule against the markdown, so it passed all eleven of them; the ban is what closes a gap the slug rule cannot |
| `test_a_links_text_never_wraps.py` | no inline markdown link under `docs/` carries a newline -- not in its bracketed text, not in its target -- where the target is a relative path | `jekyll-relative-links` is what rewrites `](CONCEPTS.md#a)` into the built URL, and it finds links with a regular expression whose `.` **excludes a newline**. A link whose text wraps is therefore never rewritten: the `](FILE.md)` reaches the HTML verbatim, Jekyll has also copied `FILE.md` to the site as a static file, and the reader gets raw markdown with a `#fragment` that addresses nothing. There is no 404 for anyone to report, and github.com renders the same characters correctly -- which is the surface it gets reviewed on. Measured by building the site on 2026-08-07: **35 such links, 17 carrying a fragment**, 20 of them in `ADOPTING-THESE.md`. `test_internal_links_resolve.py` is blind to the class by construction, because it computes github's rule and scans one line at a time, so all 35 passed it. That blindness was load-bearing for a second control: one of the 35 anchored into a `--` heading, the exact shape PR #16 banned, and the wrap hid it from the ban as well. The **target** half of the rule refuses nothing today and is kept because the tempting summary -- "only the text matters" -- is false: a probe page through the real build shows a wrapped target is not rewritten either, by a second route, the page lookup |
| `test_prose_rules_hold.py` | the three banned constructions in `docs/HOUSE-STYLE.md` that can be matched without judgment are absent from every tracked page, and two measured qualities -- sentences over 30 words, table cells over 40 -- do not get worse than their baseline | `HOUSE-STYLE.md` stated the writing rules and **nothing enforced any of them**, so the only thing between the corpus and a slow drift back was whether a reviewer remembered a rule identifier. The design constraint is what makes this file worth reading: hard-fail **only** where a violation is unambiguous, because a gate that reddens a legitimate editorial choice is one people delete -- a judgment this repository already recorded once, in `TheBlufConventionHasOneSpelling`. So every candidate was measured over the corpus and every hit was read before being enforced, and **B-7 was rejected on that evidence**: "cleanly" fires 16 times and not one is the self-praise the rule bans -- `git merges both cleanly`, `a session that exits cleanly`, `an alias stub that resolves cleanly`. The rule turns on *what is being described*, which no pattern over the text can see. `B-10` went the same way on its single hit. The scanner rejoins wrapped lines into paragraphs first, and that is not a detail: `HS-14` wraps prose near 100 characters, so a line-based scan reported **zero** sentences over 30 words across 110,000 words, and read a line beginning with a wrapped "importantly" as a sentence opener. Both were observed here before it was rewritten. `HOUSE-STYLE.md` must be able to quote what it bans, so a table row whose first cell is a rule identifier is exempt -- that row and nothing more, or the rule sheet becomes the one unchecked page in the repository |
| `test_session_mail.py` | the file-drop lane end to end, **executed**: that the queue lives inside `.git` and cannot be committed; that sender and drain derive the box key from ONE function and resolve to the worktree root before hashing; that showing is not consuming; that the marker is minted after the emit; that the claim is an exclusive open; that every cap is measured AS RENDERED; that silence is never ambiguous; that an expired message is not lost quietly; and that the urgent tier is absent rather than half-built | the failure that costs most leaves every observable saying it worked. A message addressed to a peer's PRIMARY CHECKOUT rather than its worktree queued, reported success, and landed in a box nobody drains -- so the two ends must start from the same path, which by default they do not. The cap arm has the same shape: a 34,539-byte injection passed an 8,000-byte cap reporting zero truncated, because the raw body was charged while the renderer added its own bytes per line. The three named classes are the failures `docs/SESSION-MAIL.md` says survive a plausible first pass, and a build that has not been tested against them looks finished |
| `test_a_cannot_tell_never_reads_as_an_all_clear.py` | three coordination surfaces, **executed** against throwaway repositories rather than scanned: `overlap.ps1 -File` states its all-clear and says how many peer worktrees it examined; `presence.ps1 -Json` writes a stderr receipt when the roster is unavailable, while stdout stays `[]`; `remove.ps1 -DeleteBranch` acts on the branch the worktree was **on** and relays git's own refusal instead of asserting one | this repository's central claim turned on its own tooling: every failure mode here is byte-identical to success. `overlap.ps1`'s human branch printed **nothing** for "nobody else is in this file" -- the same nothing it prints when it dies before answering -- one line below the `-Json` branch that had already been fixed for precisely that, and it is the command the docs say to run *before* starting a chunk of work. `presence.ps1` wrote a bare `[]` outside a repository, alone among that file's unavailable paths in carrying no receipt, and it is the roster other tools gate on. And `remove.ps1` asserted a single cause -- "not merged into its upstream" -- for **any** `git branch -d` failure, while asking git to delete `-Name`, the worktree DIRECTORY component, which for the documented `new.ps1 -Name my-task -Branch feature/my-task` is not a branch name at all: the real branch survived a run the human read as a full cleanup, and a *detached* worktree deleted the unrelated branch that happened to share its directory name. Every case here is paired with a negative control, because a script that prints a reassuring line unconditionally passes an all-clear test perfectly. A second pass over the same file -- applying the rule the first pass wrote down, *look for the sibling path* -- found two more. An empty map served **from the cache** rendered a phantom worktree, because zero rows serialise as `"rows": null` and `@($null).Count` is 1: it invents a peer rather than omitting one, and disagrees with the fresh walk seconds earlier, so it reads as flakiness rather than as a defect. And a run that resolved no git repository exited **0** with `[]`, which `collision_gate.ps1` reads as "resolved, nobody is touching it" -- which is why one case here drives the **real gate** rather than overlap alone: the gate discards stderr, so a receipt cannot reach it and only the exit code can |
| `test_an_off_row_declares_whether_it_fails_the_run.py` | every site in `bin/ccx-doctor.ps1` that can report a control `OFF` is in a table saying whether it fails the run, the verdict still partitions `OFF` on the `required` field rather than on the tag, every tag in the vocabulary has a declared effect, and `INSTALL.md` names each opt-in `OFF` row | the doctor reported the ASCII gate `OK` -- its tag for "installed, wired, and it refused what it must refuse" -- because the checker FILE was present, while printing in capitals two hundred lines later that nothing wires it. Its own status field contradicting its own blind spot, in the command whose purpose is answering whether the controls are real. **The near-miss is the reason this is a table and not a one-line pin.** The vocabulary promises `OFF` costs nothing for a control with no installer, and no code reads that sentence: the verdict partitions on a separate `-Required` field **defaulting to true**, so re-tagging that row `OFF` and nothing else would have turned exit 0 into exit 1 on every clean machine. Ten of the twelve `OFF` sites inherit that default silently. Source is read, never run -- `gates.yml` says in its header why CI does not run the doctor, and a green CI run that skipped its live-session paths would be a worse signal than none |
| `test_redirect_covers_every_page.py` | every document the new site serves has a stub on the **old** host forwarding to its exact new URL, every stub's destination is read from `docs/_config.yml` rather than written down again, and no stub runs a script | the site moved off `github.io` on 2026-08-10 because that host is blocked by the firewalls in front of the readers it is written for, and the old address keeps answering rather than being switched off -- every link written before the move still names it. A forwarder fails silently in both directions. A document added later gets a page on the new site and nothing on the old one, so an old link returns a 404 **from a site that is still up**, which reads as "no such page" rather than "it moved"; and a stub that forwards to the wrong host sends the reader somewhere with the browser reporting success throughout. So the page list is derived from the same `git ls-files docs/*.md` the site build uses and the destination has exactly one definition. The stub's own no-script requirement is the third, and it outlived the site-wide rule it used to cite: `docs/assets/style.css` retired that on 2026-08-16 and the site now ships `assets/site.js`. This one stands on what a stub IS -- its entire content is the address of the page that moved, so one 404 page reading its own path would replace all seventeen files and, with script disabled, could only offer the site root without saying which document was wanted |
| `test_word_copy_tracks_the_markdown.py` | the committed `docs/word/KORUS.docx` is what its markdown produces: the page is rebuilt with the pandoc invocation this test parses out of its own docstring, and the two are compared on extracted text, external link targets in order, and the heading-level sequence | the Word copy is GENERATED and nothing regenerates it, so an edit to the page leaves a stale document published beside a current one, carrying no sign of its age -- and that page changed three times on 2026-08-13 alone. Bytes are not compared and neither are zip members: every entry carries the build clock, and hashing `word/document.xml` reddens on the CONVERTER rather than the document, naming no sentence a reader can act on. The three projections are each other's blind spots -- demoting a heading from `##` to `###` keeps every word while dropping the section from the table of contents, and a link's TARGET is not in the body at all, since `word/document.xml` stores `r:id` and the URL lives in `word/_rels/document.xml.rels`. Pandoc missing is a FAILURE and not a skip: the artifact is published, and a skip over it reads as a pass |
| `test_script_only_enhances.py` | every control `docs/assets/site.js` adds -- the copy buttons, the search box, the nav toggle -- is **created by the script** and appears in no layout; the stylesheet has a rule for each; every `<script src>` is same-origin and deferred; and `docs/search.json` builds from `site.pages` and computes no heading anchor; and an illustration nobody should run -- the landing page's `git checkout -B`, the two blocks labelled WRONG in Tips and tricks, the refusal sample in the Quickstart -- carries the `<!-- no-copy -->` marker that suppresses its Copy button | the no-script rule was retired on 2026-08-16, and this is the half of it that survived. The old rule cost something measurable -- six independent reviews of the live site each asked for copy buttons and a collapsing nav, and the rule was the only reason neither existed -- but the property it protected is real: **a reader with script disabled must meet no control that does nothing.** The obvious next edit breaks it silently. Someone puts `<button class="copy-button">` in the layout because markup is easier than creating an element, every check here stays green, the site builds, the links resolve, the prose passes, and the only reader who can see the defect is the one nobody tests as. The same-origin case is older than the script rule and unrelated to it: the firewall in front of this site's readers blocks third-party hosts, so a CDN is not a convenience, it is a script that never loads on a page that then looks broken in a way nothing local can reproduce. The search index is pinned to page-level results because an anchor computed by Liquid's `slugify` and one rendered by kramdown disagree on any heading containing `--` -- 19 of 766 measured -- and a result carrying the wrong slug still returns 200, still renders, and lands the reader at the top of the page while promising a section |
| `test_the_reaper_re_reads_the_fence_per_removal.py` | `prune-merged.ps1`, **executed** against a throwaway repository with three prunable siblings and a shimmed fence: a session arriving in the second candidate while the first is being removed stops that removal and the batch still finishes the third; the receipt's `readsAtApply` equals the candidate count and matches a count taken outside the script; a fence that dies mid-batch stops the rest, reports `availableAtApply: false`, and exits 1 rather than 2 once something has already been removed. One static case reads the source instead, so the file still measures something where `pwsh` or `git` is absent | the second occupancy read exists because the decision pass spends a gh round trip per candidate and a session can arrive inside that window -- and it was taken **once, before the loop**, so it was fresh for the first removal and stale for every one after it, by exactly the interval it exists to close. Measured on this fixture against the shipped code: all three worktrees removed, the occupied one included, `vetoedCandidates: 0`, and nothing in the receipt distinguishing a check made against a snapshot older than the removal before it. The comment above the call had been corrected to admit this, so the code was honest and still wrong. **A batch of one cannot show it**, which is why it survived: the bug needs a run that removes two worktrees, and that is the run with the most to lose. The negative control is not optional here -- a green "the arrival was seen" is equally consistent with a fixture whose planted record the fence could never see, so the same record is planted before a run and the decision pass must catch it. The sticky case exists because moving the read inside the loop is what breaks the other half: with one snapshot per run "a fence that dies mid-run stops the rest" held by accident, since the unavailable verdict was loop-invariant, and a naive move makes a fence that recovers by candidate 4 resume destroying things after an unexplained gap while the receipt reports the last read and calls it available |
| `test_the_landing_page_stays_a_front_door.py` | **HS-23**: `docs/index.md` stays under a 1,400-word cap, names `QUICKSTART.md` inside its opening, and carries no script inventory -- **and** the three pages it split into still carry what they received, with `LIMITS.md` and `SCRIPTS.md` each handing the reader onward to the quickstart | the landing page had reached **3,143 words** by 2026-08-16, and no sentence on it was wrong. The install procedure, the requirements table, the vendor-surface limits and a 40-row script inventory had each arrived there for the same good reason -- it was the page everyone read -- and each was correct on arrival. **The defect was ordering, and every gate in this repository was green throughout**, because they all ask whether a statement is true and none of them asks whether a reader can find it: a developer deciding whether to try this met the caveats before the commands and the first command 68% of the way down. The receiving half is not decoration. A cap alone is satisfied perfectly by DELETING the content instead of moving it, which is the failure `PD-1` to `PD-7` exist to refuse, so the pages that took the material are checked for still holding it -- a landing page trimmed by moving passes both classes and one trimmed by cutting passes only the first. The inventory pattern matches a table cell naming a script by PATH rather than the word "script", because the page legitimately names three of them in the table saying which mechanism prevents, repairs and detects the flagship failure, and a scan that could not tell that row from an inventory row would redden the fix |
| `test_a_number_is_claimed_by_creating_a_file.py` | `alloc.ps1`, **executed** against throwaway repositories: a sibling arriving inside the window between the floor sweep and the open keeps its number and its claim file; eight concurrent processes return eight distinct numbers, all exit 0, and at least two of them genuinely overlapped; a second configured sequence keeps its own registry, floor and padding, and neither ledger moves the other; a number living only on an unpushed branch is not reissued; a sibling holding the high-water mark cannot kill an allocation while an unreadable one refuses; and a config whose two entries could describe one namespace is refused before the registry is touched | reading the highest number and adding one is the tempting way to allocate, and two sessions that both read take the SAME number, write DIFFERENTLY-named files, and merge CLEAN -- no textual conflict, so git cannot see it and neither can review. It corrupted the ledger three times in the repository this tooling came from. The timing cases alone do not defend it: measured here, mutating the claim from `CreateNew` to `Create` left all eight racers distinct and every clock-based case green, because the floor already skips a number a claim file exists for, so nothing ever reaches the open twice. The collision is therefore SUPPLIED -- one line inserted into a copy of the claim loop, wrapping the shipped `File.Open` rather than replacing it -- and that case goes red on the mutation every time. The ratchet cases are the same shape and the same discovery: eight simultaneous allocations once returned SEVEN numbers, one process dying on the shared high-water file inside the floor sweep, before it had claimed anything, with no duplicate and nothing looking corrupt |
| `test_the_red_watcher_starts_one_seat_per_proven_red.py` | `scripts/cron/watch-ci-red.ps1`, **executed** against a throwaway repository with a stub GitHub client, a stub seat and the **real** claim registry: a repository with nothing red starts nothing, a red starts exactly one seat, a second tick over the same red starts none, a claim held by another worktree blocks, a tick that finds its own seat gone says `SEAT-GONE` and fails while a tick that finds it running says `ALREADY-CLAIMED` and passes, and each of the three controls refuses with exit 2 instead of reporting a zero it could not establish. Four more cases pin the receipt itself, which is the only interface a scheduled run has: a talking seat cannot land inside the JSON, a contended pass lock still produces a receipt rather than exit 1 and an empty stdout, a red missing from a **capped** open list is not read as closed, and a claim that cannot be confirmed is `INCOMPLETE` rather than spending the code reserved for a failed look. At least seven static cases read the source, so the file still measures something where `pwsh` or `git` is absent | the watcher exists because nothing tells a session that a required check went red, and every one of its four properties fails **silently** when lost. A watcher that starts a seat while quiet has become the resident session it replaced, at **2,108 metered tokens per waiting minute** on a three-minute heartbeat and 22,275 on a ten-minute sleep loop -- and the run still looks correct, because it did notice the red it was going to notice anyway. A second seat on one red is worse than none, since each assumes the other did not, and `claim.ps1` **cannot** catch that case on its own: re-taking a key you already hold is a documented success, so every tick from the same worktree re-takes its own claim and exits 0. The refusal cases are the measured one. On 2026-08-31, `CLAUDE_CONFIG_DIR` pointing at a directory that does not exist made `claude agents --json` return an empty list and exit 0 -- no error, no warning, a mistyped root byte-identical to an empty fleet -- and the same shape is available here three ways, of which the label is the sharpest: a query for a label nobody created returns an empty list and exits 0, so a repository that never installed the labelling half reads exactly like one with nothing red. **Every case asserting an absence is paired with one asserting the presence** on the same fixture with one variable changed, because "nothing was started" is equally consistent with a stub seat the script could never reach |
| `test_every_validation_check_is_proven_by_a_control.py` | every check in `scripts/validation/` is registered in `run-checks.ps1`, names both a broken and a clean fixture, and -- executed -- comes back **BROKEN** on the first and **CLEAN** on the second; and the runner, run over a copy whose broken fixture has been gutted, refuses to measure anything live and exits 3 | those checks exist to fire when the multi-session tooling misbehaves, and an instrument validated only against a healthy estate cannot tell a clean result from a broken probe. Measured 2026-08-31 on Claude Code CLI 2.1.251: `claude agents --json` under a nonexistent config root returns `[]` and exit 0, so an empty fleet and a mistyped root are byte-identical -- and the session that measured it nearly reported its own quoting bug's two empty answers as a real reading. **Two fixtures and not one**, because a check wired to always fail passes the broken case perfectly and one wired to always pass passes the clean case perfectly; only the pair rules out both. The registry cases are static, so this file still measures something where `pwsh` is absent, and the runner's refusal gets a planted control of its own -- a refusal path nobody exercises is not a control |
| `test_a_validation_check_reads_real_data_the_way_it_reads_a_fixture.py` | the three `scripts/validation/` checks that read a live corpus, **executed** against corpora this file builds: a mail root with a chosen number of receipts, and a real two-branch git repository with an index row. A lane with zero receipts is measured rather than crashed, a receipt naming a longer id does not count as delivery of a shorter one, two receipts still match an id that differs only in case, one instant spelled `Z` and spelled with an offset gets one verdict, and an index row naming only the original does not excuse an undeclared second path | `run-checks.ps1` proves each check against a FIXTURE, and that word covers the verdict logic and nothing else. A fixture is hand-written JSON or a tab-separated ref list, so it never reaches the code that reads a real machine -- an instrument mutated so it can never report a collision on real data still read **PROVEN** across all five checks. Three defects lived in exactly that gap and each is a case here. `Get-CcxRenderedIds` returned a HashSet into the pipeline, so PowerShell unrolled it and the function had three return types by receipt count: **0 receipts crashed the check**, which is the dead-drain shape the signal exists for, 1 made membership a substring match, and 2+ lost the case-insensitive comparer. Every message fixture holds exactly one receipt, the single count where the wrong type happens to answer correctly. A `Z` timestamp came back `Kind=Utc` and was compared against a local clock, hiding lateness up to the machine's offset -- every fixture timestamp is eight months stale, so no offset could flip one. And the index-row exclusion allowed one undeclared path, on a branch no fixture reaches because no fixture supplies an index file |
| `test_a_validation_check_never_calls_an_empty_corpus_clean.py` | every registered check, handed a corpus that exists and holds nothing, answers **CANNOT_TELL** and exits 2 rather than 0; every JSON receipt carries `examined`, `corpus` and `brokenWhen`; a clean text run still prints what it scanned; and no check names the CLEAN verdict itself | a count from a pipeline carrying a head, a tail or a sample cap is not a census, and a truncated result reads as a complete one. The failure is the same one `test_a_cannot_tell_never_reads_as_an_all_clear.py` pins for the coordination surfaces, one directory over: an answer of "I found nothing to read" rendering as "I looked, and it is clear". The completeness case is what makes it stick -- the switch table is compared against the runner's registry in both directions, so a check added tomorrow cannot escape the rule by being new, and the verdict case reads where CLEAN is decided so a second copy of that rule cannot appear unnoticed |

The requirement is read off the **consumer** wherever possible -- what the checkers actually import,
what the gate actually dot-sources, where the installer actually writes its allowlist -- rather than
from a list of names typed into a test file. A copy list checked against a hand-written list only
proves that two lists agree.

---

## What they do NOT cover

Read this part. A test suite's blind spots are the reason a green run can still be wrong.

* **They do not run either installer.** The installers refuse to run inside an agent session, by
  design, and nothing here defeats that. So no case observes a hooks directory after an install. The
  evidence that a copy actually landed comes from `bin/ccx-doctor.ps1`, which hashes the installed
  substrate and the installed gate's helpers against this checkout, and from
  `install-git-hooks.ps1 -Status` / `install-gate.ps1 -Status`.
* **They do not test the installed copies.** Everything here binds files in this checkout. Enforcement
  runs from an installed copy that can be older, and a suite that only ever binds the repository's
  copy is exactly how a stale gate stayed green. The doctor is what compares the two.
* **They do not fire the git hooks, the claim gate, the push guard, the collision gate, the announce
  hook, the self-heal backstop or the ASCII checker.** The doctor attacks each of those against a
  throwaway fixture and requires it to refuse, with a paired negative control. That is a different
  kind of evidence and it is not duplicated here.
* **The PowerShell parsing is textual.** Comments are stripped before any declaration is read (a
  commented-out matcher used to slip through, and was caught by mutating the tree on purpose), but
  these are regular expressions over source, not a PowerShell parser. Restructuring a declaration --
  building the matcher list in a loop, say -- makes the extractor raise rather than quietly return
  nothing, which is the failure direction to prefer, but it does mean a reshape needs the test
  updated deliberately rather than deleted.
* **The `pre-commit` scan is line-oriented.** It flags a file operation naming `pre-commit` on the
  same line as the operation. A write split across lines, or one routed through a variable holding
  the name, would not be seen.
* **The link checks never leave the filesystem.** A relative link correct in this checkout still
  breaks on the published site if the page was not deployed, and nothing local can see that. Nor is
  link *text* checked against the section it leads to: a link reading "the leak gate" that resolves
  to a heading on the review-depth page passes, because both ends exist.
* **The cases that execute PowerShell SKIP when `pwsh` or `git` is absent, and a skip prints beside
  the passes.** `test_worktree_gate_no_args.py`, `test_a_cannot_tell_never_reads_as_an_all_clear.py`,
  `test_the_reaper_re_reads_the_fence_per_removal.py` and
  `test_a_number_is_claimed_by_creating_a_file.py`
  run real scripts against throwaway repositories, so on a host without those two binaries they
  measure nothing and say so quietly. That is the opposite of the pandoc rule above, and the reason
  for the difference is that pandoc's absence would leave a *published artifact* unchecked while
  these leave only a script that cannot run on that host at all. CI has both; a local run may not.
* **No coverage of the coordination installer, the claim ledger, or the leak gate.** Absence here is
  not a verdict on those; it is absence. The allocator left this list on 2026-08-31 and only
  partly: `test_a_number_is_claimed_by_creating_a_file.py` pins the claim step, the per-sequence
  partition, the all-refs and working-tree floor terms and the ratchet's two failure directions --
  nothing about `-ShowFloor`'s reporting, the index-row term, or the 500-attempt limit. Pruning is
  reached only by
  `test_the_reaper_re_reads_the_fence_per_removal.py`, which runs the reaper against a throwaway
  repository and pins that the occupancy fence is re-read before *each* removal, that a fence which
  dies mid-batch stops the rest, and that the receipt's read count is real -- nothing about whether
  the merge test, the cleanliness test or the branch deletion are *right*. `presence.ps1` and `overlap.ps1` are
  reached only by `test_a_cannot_tell_never_reads_as_an_all_clear.py`, which pins their
  cannot-tell paths and the shape of the `-Json` row -- nothing about whether the liveness fence, the
  two-dot/three-dot intersection, or the worktree attribution are *right*.

Every extractor raises when it finds nothing, rather than returning an empty set for a caller to
compare against another empty set -- two empty sets are equal, and that is the shape of a test that
passes having checked nothing.
